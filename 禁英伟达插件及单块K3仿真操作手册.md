# Sionna-RK 禁用 NVIDIA 插件及单块 K3 仿真操作手册

> **适用设备**：进迭时空 K3 Pico-ITX，RV64 / `riscv64`  
> **已验证系统**：Ubuntu 26.04.1 LTS，Linux 6.18.3-7-spacemit-generic  
> **Sionna-RK 官方基线**：`NVlabs/sionna-rk`，提交 `0abdce3`  
> **OpenAirInterface 基线**：`2025.w34`  
> **首次移植验证日期**：2026-08-29  
> **本手册最近更新**：2026-08-30（补入 UE/gNB 双向消息实验与仿真参数说明）  
> **操作方式**：全部命令均直接在 K3 本机终端执行，不需要其他主机、SSH、SCP 或外部补丁文件。

---

## 0. 本次更新内容与快速入口

本文件是主机工作目录中的最新操作手册，完整路径为：

```text
C:\Users\Administrator\Desktop\K3移植Sionna-RK\禁英伟达插件及单块K3仿真操作手册.md
```

2026-08-30 已重新整理并写入以下内容：

1. 在完整移植补丁中新增 `scripts/k3-rfsim-message-test.sh`；
2. 在 `scripts/run-k3-rfsim.sh` 中新增 `message-test` 子命令；
3. 新增第 24 节，说明 UE 发送 `hello`、gNB 返回 `world` 的双向 UDP 用户面实验；
4. 新增第 25 节，整理 RFsim 连接、传播时延、IQ 保存、信道模型、n78 载波、PRB、numerology、增益、PRACH、PLMN 和时序等可调参数；
5. 说明同一块 K3 上双隧道地址实验所需的临时 `sysctl` 设置，以及脚本退出时自动恢复的机制；
6. 更新第 27 节最终判据，加入 `hello/world` 双向用户面消息测试通过；
7. 重新生成并嵌入完整补丁，SHA-256 更新为：

```text
b35232cc7ec640be1b7a899fcfb9fd5887f86748376095da06b2883886d55738
```

完成 gNB 和 nrUE 启动后，双向消息实验的快速命令为：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh message-test
```

成功输出应为：

```text
UE -> gNB: sent 5 bytes; received hello from 10.0.1.2:5002
gNB -> UE: sent 5 bytes; received world from 10.0.1.1:5001
PASS: bidirectional application messages crossed the OAI RFsim data bearer.
```

详细原理、前置条件、修改消息方法和参数表请直接查看第 24、25 节。

---

## 1. 目标、边界和最终验证结果

本手册从一块只有官方 Sionna-RK 仓库或尚未下载仓库的 K3 开始，完成以下工作：

1. 将 Sionna-RK 的默认 GPU 工作流改成 RV64 CPU-only 工作流；
2. 禁止 CUDA、TensorRT、NVIDIA Container Runtime、MPS、DGX 优化和 x86 AVX 优化；
3. 下载固定版本的 OpenAirInterface（OAI）；
4. 修改 OAI，使其能够在 K3 的 `riscv64` 架构上安装依赖和编译；
5. 构建 RV64 原生的 `nr-softmodem` 与 `nr-uesoftmodem`；
6. 在同一块 K3 的两个本地终端中分别运行 gNB 和 nrUE；
7. 通过本机 `127.0.0.1:4043` RFsim 链路完成 PBCH 解码和 UE 初始同步；
8. 通过 `oaitun_ue1` 与 `oaitun_enb1` 完成 UE→gNB 的 `hello` 和 gNB→UE 的 `world` 双向 UDP 用户面数据实验。

已在第一块 K3 上实测获得：

```text
Client connects from ::ffff:127.0.0.1
Connection to 127.0.0.1:4043 established
Initial sync: pbch decoded sucessfully, ssb index 0
Initial sync successful, PCI: 0
UE synchronized!
UE RNTI 1234 ... in-sync
UE -> gNB: sent 5 bytes; received hello from 10.0.1.2:5002
gNB -> UE: sent 5 bytes; received world from 10.0.1.1:5001
PASS: bidirectional application messages crossed the OAI RFsim data bearer.
```

本阶段不包括：5GC 注册、PDU Session、真实射频前端、两块 K3 跨网络运行或 NVIDIA GPU 插件功能。

---

## 2. 为什么必须修改官方仓库

官方 Sionna-RK 面向 NVIDIA GPU 环境，原始工作流包含 CUDA、TensorRT、MPS、NVIDIA 容器和 GPU 插件。K3 是 RV64 CPU 平台，不具备这些运行条件。

同时，Sionna-RK 使用的 OAI 固定版本原本只接受常见 x86/ARM 架构，并包含若干 x86 SIMD、用户态硬件计数器和动态插件假设。直接在 K3 上运行会依次遇到：

```text
ubuntu26.04 is not supported
compile for CPU architecture, CMAKE_SYSTEM_PROCESSOR riscv64
asn1c Segmentation fault
x86 intrinsic/type not found
CPU-only 插件符号未定义
Illegal instruction: rdcycle
libdfts.so: undefined symbol: dft_implementation
```

因此需要同时修改两层：

- **Sionna-RK 根仓库**：提供 CPU-only 开关、K3 依赖脚本、原生 OAI 构建脚本，并关闭 NVIDIA 入口；
- **OAI 补丁**：增加 RV64 架构支持、标量/SIMDe 回退、CPU-only 插件空实现和运行阶段修复。

---

## 3. 两种复现方式

本文同时提供两种方式，修改内容完全相同。

### 3.1 方式 A：K3 本机生成完整补丁并一次性应用（推荐）

优点：

- 不依赖其他主机或文件；
- 避免手工复制几十个修改片段时漏行；
- 可以先用 `git apply --check` 验证；
- 可重复、可审计、可回滚。

第 8 节包含完整补丁。只需在 K3 终端通过 here-document 生成文件，再用 `git apply` 应用。

### 3.2 方式 B：逐文件理解和修改

第 9～12 节解释每个文件为什么修改、修改后应该具备什么行为，并给出逐文件检查与应用方法。完整精确差异仍以第 8 节补丁为准，避免文档中出现两份可能不一致的代码。

建议第一次复现使用方式 A，随后阅读方式 B 理解修改。

---

## 4. K3 本机终端准备

本手册要求在 K3 上打开两个本地终端窗口。安装和构建阶段只需使用一个；最终仿真阶段两个终端分别运行 gNB 和 nrUE。

先检查系统：

```bash
uname -a
uname -m
cat /etc/os-release
nproc
free -h
df -h /
ip -br addr
```

必须确认：

```text
riscv64
```

建议至少预留 20 GB 空间，并保证供电、散热和网络稳定。

启用网络时间同步：

```bash
sudo timedatectl set-ntp true
timedatectl
```

安装基础工具：

```bash
sudo apt-get update
sudo apt-get install -y \
  git \
  ca-certificates \
  build-essential \
  cmake \
  ninja-build \
  pkg-config \
  python3 \
  python3-pip \
  python3-venv
```

检查：

```bash
git --version
gcc --version
cmake --version
python3 --version
```

---

## 5. 获取官方 Sionna-RK 并固定版本

如果 K3 尚未下载仓库：

```bash
cd ~
git clone https://github.com/NVlabs/sionna-rk.git
cd ~/sionna-rk
```

如果已经有官方仓库：

```bash
cd ~/sionna-rk
git remote -v
git status --short --branch
```

确保工作树没有需要保留的本地修改，然后固定到已验证提交：

```bash
git fetch --all --tags
git checkout 0abdce3
git switch -c k3-rv64-cpu-only
```

确认：

```bash
git rev-parse --short HEAD
git status --short --branch
```

预期：

```text
0abdce3
## k3-rv64-cpu-only
```

如果分支已存在，使用：

```bash
git switch k3-rv64-cpu-only
```

不要把本补丁应用到未知提交或已有修改的工作树。

---

## 6. 设置 CPU-only 环境变量

当前终端先执行：

```bash
export SIONNA_RK_CPU_ONLY=1
export CUDA_VISIBLE_DEVICES=""
export NVIDIA_VISIBLE_DEVICES=void
```

写入当前 K3 用户的 `~/.bashrc`：

```bash
grep -q 'Sionna-RK on K3: force RV64 CPU-only mode' ~/.bashrc || cat >> ~/.bashrc <<'EOF'

# Sionna-RK on K3: force RV64 CPU-only mode
export SIONNA_RK_CPU_ONLY=1
export CUDA_VISIBLE_DEVICES=""
export NVIDIA_VISIBLE_DEVICES=void
EOF
```

重新加载：

```bash
source ~/.bashrc
```

验证：

```bash
printf 'SIONNA_RK_CPU_ONLY=<%s>\n' "$SIONNA_RK_CPU_ONLY"
printf 'CUDA_VISIBLE_DEVICES=<%s>\n' "$CUDA_VISIBLE_DEVICES"
printf 'NVIDIA_VISIBLE_DEVICES=<%s>\n' "$NVIDIA_VISIBLE_DEVICES"
```

预期：

```text
SIONNA_RK_CPU_ONLY=<1>
CUDA_VISIBLE_DEVICES=<>
NVIDIA_VISIBLE_DEVICES=<void>
```

不要安装 CUDA Toolkit、TensorRT、NVIDIA Container Toolkit、MPS、L4T 或 `nvidia-modelopt`。CPU-only 模式的目的不是让这些软件在 RV64 上勉强安装，而是完全绕过它们。

---

## 7. 应用补丁前建立恢复点

```bash
cd ~/sionna-rk
git status --short
git tag -f k3-before-port 0abdce3
```

此时 `git status --short` 应为空。

如果后面需要从头恢复：

```bash
cd ~/sionna-rk
git reset --hard k3-before-port
git clean -fd
```

`git clean -fd` 会删除未跟踪文件，只能在确定没有个人文件时执行。

---

## 8. 方式 A：在 K3 本机生成并应用完整移植补丁

### 8.1 生成补丁文件

在 K3 的 `~/sionna-rk` 中执行下面整段命令。必须从第一行复制到最后一行，结束标记 `K3_RV64_PATCH` 必须单独占一行。

```bash
cd ~/sionna-rk

cat > ~/sionna-rk-k3-rv64.patch <<'K3_RV64_PATCH'
diff --git a/Makefile b/Makefile
index 24bf9d5..6608eeb 100644
--- a/Makefile
+++ b/Makefile
@@ -3,35 +3,34 @@
 ## SPDX-License-Identifier: Apache-2.0
 ##
 
-GPU=
-ifdef gpus
-    GPU=--gpus=$(gpus)
-endif
-export GPU
+CPU_ONLY ?= 1
+export SIONNA_RK_CPU_ONLY := $(CPU_ONLY)
 
 .PHONY: doc prepare-system sionna-rk build-gnb
 
 prepare-system:
 	./scripts/configure-system.sh
-	./scripts/build-custom-kernel.sh
-	./scripts/install-custom-kernel.sh
-	echo "Reboot to load the new kernel and continue the installation."
+	@if [ "$(CPU_ONLY)" != "1" ]; then \
+		./scripts/build-custom-kernel.sh; \
+		./scripts/install-custom-kernel.sh; \
+		echo "Reboot to load the new kernel and continue the installation."; \
+	else \
+		echo "CPU-only mode: skipping NVIDIA L4T custom kernel steps."; \
+	fi
 
 sionna-rk:
 	./scripts/quickstart-oai.sh
 	./scripts/generate-configs.sh
 	./plugins/common/build_all_plugins.sh --host
-	./plugins/common/build_all_plugins.sh --container
 
 build-gnb:
-	./scripts/build-oai-images.sh --debug ext/openairinterface5g
+	./scripts/build-oai-native.sh ext/openairinterface5g
 
 doc: FORCE
 	cd doc && ./build_docs.sh
 
 test:
 	./plugins/common/build_all_plugins.sh --host
-	./plugins/common/build_all_plugins.sh --container
 	./plugins/testing/run_all_tests.sh --host
 
 FORCE:
diff --git a/patches/config/config.b200.patch b/patches/config/config.b200.patch
index ecd1e81..0f8a10a 100644
--- a/patches/config/config.b200.patch
+++ b/patches/config/config.b200.patch
@@ -3,7 +3,7 @@ new file mode 100644
 index 0000000..f71e8f6
 --- /dev/null
 +++ .env
-@@ -0,0 +1,86 @@
+@@ -0,0 +1,83 @@
 +# =============================================================================
 +# Config Files
 +# =============================================================================
@@ -11,16 +11,13 @@ index 0000000..f71e8f6
 +SYSTEM_CONFIG="../common/sys_config.yaml"
 +GNB_CONFIG="../common/gnb.sa.band78.24prbs.conf"
 +
-+CUDA_MPS_PIPE_DIRECTORY=/tmp/nvidia-mps
-+CUDA_MPS_LOG_DIRECTORY=/tmp/nvidia-log
-+
 +# =============================================================================
 +# Image Tags
 +# =============================================================================
 +
-+GNB_IMAGE=oai-gnb-cuda
++GNB_IMAGE=oai-gnb
 +GNB_TAG=latest
-+UE_IMAGE=oai-nr-ue-cuda
++UE_IMAGE=oai-nr-ue
 +UE_TAG=latest
 +AMF_TAG=v2.1.10
 +SMF_TAG=v2.1.10
diff --git a/patches/config/config.common.patch b/patches/config/config.common.patch
index 112f323..5719318 100644
--- a/patches/config/config.common.patch
+++ b/patches/config/config.common.patch
@@ -139,7 +139,7 @@ index d531b15..a766cc0 100644
          depends_on:
              - oai-upf
          networks:
-@@ -84,320 +91,114 @@ services:
+@@ -84,320 +91,110 @@ services:
                  ipv4_address: 192.168.72.135
          healthcheck:
              test: /bin/bash -c "ping -c 2 192.168.72.134"
@@ -153,10 +153,9 @@ index d531b15..a766cc0 100644
 -        container_name: rfsim5g-oai-gnb
 -        cap_add:
 -            - SYS_NICE
-+        image: ${GNB_IMAGE:-oai-gnb-cuda}:${GNB_TAG:-latest}
++        image: ${GNB_IMAGE:-oai-gnb}:${GNB_TAG:-latest}
 +        container_name: oai-gnb
 +        user: ${USER_ID:-0}:${GROUP_ID:-0}
-+        runtime: nvidia
          cap_drop:
              - ALL
 +        cap_add:
@@ -181,8 +180,6 @@ index d531b15..a766cc0 100644
 +            - /dev/bus/usb/:/dev/bus/usb/
          volumes:
 -            - ../../conf_files/gnb.sa.band78.106prb.rfsim.yaml:/opt/oai-gnb/etc/gnb.yaml
-+            - /tmp/nvidia-mps:/tmp/nvidia-mps
-+            - /tmp/nvidia-log:/tmp/nvidia-log
 +            - ${GNB_CONFIG:-../common/gnb.sa.usrpb200.24prbs.conf}:/opt/oai-gnb/etc/gnb.conf
 +            - ../../plugins:/opt/oai-gnb/plugins # mount all plugins for weights, logs, data capture, etc.
 +        ipc: host
@@ -199,9 +196,8 @@ index d531b15..a766cc0 100644
 -        container_name: rfsim5g-oai-nr-ue
 -        cap_drop:
 -            - ALL
-+        image: ${UE_IMAGE:-oai-nr-ue-cuda}:${UE_TAG:-latest}
++        image: ${UE_IMAGE:-oai-nr-ue}:${UE_TAG:-latest}
 +        container_name: oai-nr-ue
-+        runtime: nvidia
          cap_add:
              - NET_ADMIN  # for interface bringup
              - NET_RAW    # for ping
diff --git a/patches/config/config.rfsim.patch b/patches/config/config.rfsim.patch
index 5d20480..499c5f7 100644
--- a/patches/config/config.rfsim.patch
+++ b/patches/config/config.rfsim.patch
@@ -3,7 +3,7 @@ new file mode 100644
 index 0000000..229917d
 --- /dev/null
 +++ .env
-@@ -0,0 +1,86 @@
+@@ -0,0 +1,83 @@
 +# =============================================================================
 +# Config Files
 +# =============================================================================
@@ -12,15 +12,12 @@ index 0000000..229917d
 +# Changing the number of PRBs requires to also update accordingly the UE_EXTRA_OPTIONS parameter below
 +GNB_CONFIG="../common/gnb.sa.band78.106prbs.conf"
 +
-+CUDA_MPS_PIPE_DIRECTORY=/tmp/nvidia-mps
-+CUDA_MPS_LOG_DIRECTORY=/tmp/nvidia-log
-+
 +# =============================================================================
 +# Image Tags
 +# =============================================================================
 +
-+GNB_IMAGE=oai-gnb-cuda
-+UE_IMAGE=oai-nr-ue-cuda
++GNB_IMAGE=oai-gnb
++UE_IMAGE=oai-nr-ue
 +
 +GNB_TAG=latest
 +UE_TAG=latest
diff --git a/patches/config/config.testing.patch b/patches/config/config.testing.patch
index c6bf85d..d84b19e 100644
--- a/patches/config/config.testing.patch
+++ b/patches/config/config.testing.patch
@@ -15,8 +15,8 @@ index 0000000..febf5bd
 +# Image Tags
 +# =============================================================================
 +
-+GNB_IMAGE=oai-gnb-cuda
-+UE_IMAGE=oai-nr-ue-cuda
++GNB_IMAGE=oai-gnb
++UE_IMAGE=oai-nr-ue
 +
 +GNB_TAG=latest
 +UE_TAG=latest
diff --git a/patches/openairinterface5g.patch b/patches/openairinterface5g.patch
index 3f71375..805fb96 100644
--- a/patches/openairinterface5g.patch
+++ b/patches/openairinterface5g.patch
@@ -10,23 +10,30 @@ index d1c6979e76..846a6b66ce 100644
 +
 +# Generated files
 +host_product_family
-diff --git a/.gitmodules b/.gitmodules
-index 583c8d8758..b59dcaffa5 100644
---- a/.gitmodules
-+++ b/.gitmodules
-@@ -1,4 +1,5 @@
- [submodule "openair2/E2AP/flexric"]
- 	path = openair2/E2AP/flexric
--	url = ../flexric.git
--	branch = remotes/origin/2025_w34.internal
-+	url = https://gitlab.eurecom.fr/mosaic5g/flexric.git
-+	branch = remotes/origin/service-models-integration
-+
 diff --git a/CMakeLists.txt b/CMakeLists.txt
-index a71b1c3ea2..e721041ce5 100644
+index a71b1c3ea2..ece44ee7c4 100644
 --- a/CMakeLists.txt
 +++ b/CMakeLists.txt
-@@ -2142,6 +2142,16 @@ endif (${T_TRACER})
+@@ -163,13 +163,16 @@ elseif (CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
+   else ()
+     set(C_FLAGS_PROCESSOR "${C_FLAGS_PROCESSOR} -gdwarf-2 -mcpu=native -lgcc -lrt")
+   endif()
++elseif(CMAKE_SYSTEM_PROCESSOR STREQUAL "riscv64")
++  # RISC-V uses SIMDe portable fallbacks; no x86 or ARM native intrinsics.
++  set(C_FLAGS_PROCESSOR "${C_FLAGS_PROCESSOR} -gdwarf-2 -lgcc -lrt")
+ else()
+   message(FATAL_ERROR "compile for CPU architecture ${CPUARCH}, CMAKE_SYSTEM_PROCESSOR ${CMAKE_SYSTEM_PROCESSOR}")
+ endif()
+ 
+-if (NOT CROSS_COMPILE)
++if (NOT CROSS_COMPILE AND NOT CMAKE_SYSTEM_PROCESSOR STREQUAL "riscv64")
+   set(C_FLAGS_PROCESSOR "${C_FLAGS_PROCESSOR} -march=native")
+-else ()
++elseif(CROSS_COMPILE)
+   set(C_FLAGS_PROCESSOR "${C_FLAGS_PROCESSOR} -march=armv8-a")
+ endif()
+ 
+@@ -2142,6 +2145,16 @@ endif (${T_TRACER})
  include(${OPENAIR1_DIR}/PHY/CODING/nrLDPC_decoder/nrLDPC_tools/CMakeLists.txt)
  
  set(ENABLE_TESTS OFF CACHE STRING "Activate build of tests")
@@ -43,13 +50,99 @@ index a71b1c3ea2..e721041ce5 100644
  set_property(CACHE ENABLE_TESTS PROPERTY TYPE BOOL)
  set(ENABLE_PHYSIM_TESTS OFF CACHE STRING "Activate build of physim tests")
  set_property(CACHE ENABLE_PHYSIM_TESTS PROPERTY TYPE BOOL)
-@@ -2187,3 +2197,6 @@ add_subdirectory(openair2)
+@@ -2187,3 +2200,6 @@ add_subdirectory(openair2)
  add_subdirectory(openair3)
  add_subdirectory(radio)
  add_subdirectory(tests)
 +if (ENABLE_PLUGINS)
 +  add_subdirectory(plugins)
 +endif ()
+diff --git a/cmake_targets/build_oai b/cmake_targets/build_oai
+index 658b0b858a..24620ce90c 100755
+--- a/cmake_targets/build_oai
++++ b/cmake_targets/build_oai
+@@ -457,7 +457,12 @@ function main() {
+     # add some default libraries that should always be built
+     # for eNB, gNB, UEs, simulators
+     if [[ $gNB == 1 || $eNB == 1 || $UE == 1 || $nrUE == 1 || $SIMUS_PHY == 1 || $RU == 1 ]]; then
+-      TARGET_LIST="$TARGET_LIST params_libconfig coding rfsimulator dfts params_yaml vrtsim rf_emulator"
++      if [[ "$(uname -m)" == "riscv64" ]]; then
++        # Legacy LTE turbo coding contains x86 SSE/MMX-only sources; NR uses LDPC/polar targets.
++        TARGET_LIST="$TARGET_LIST params_libconfig rfsimulator dfts params_yaml vrtsim rf_emulator"
++      else
++        TARGET_LIST="$TARGET_LIST params_libconfig coding rfsimulator dfts params_yaml vrtsim rf_emulator"
++      fi
+     fi
+ 
+     mkdir -p $DIR/$BUILD_DIR/build
+diff --git a/cmake_targets/tools/build_helper b/cmake_targets/tools/build_helper
+index 49edfa91e8..6f692891f8 100755
+--- a/cmake_targets/tools/build_helper
++++ b/cmake_targets/tools/build_helper
+@@ -103,6 +103,7 @@ get_distribution_release() {
+ check_supported_distribution() {
+     local distribution=$(get_distribution_release)
+     case "$distribution" in
++        "ubuntu26.04") return 0 ;;
+         "ubuntu24.04") return 0 ;;
+         "ubuntu22.04") return 0 ;;
+         "ubuntu20.04") return 0 ;;
+@@ -575,7 +576,12 @@ install_asn1c_from_source(){
+     # Showing which version is used
+     git log -n1
+     autoreconf -iv
+-    ./configure --prefix /opt/asn1c/
++    if [[ "$(uname -m)" == "riscv64" ]]; then
++        # asn1c 0.9.29 triggers invalid pointer accesses when optimized on RV64.
++        CFLAGS="-O0 -g3" CXXFLAGS="-O0 -g3" ./configure --prefix /opt/asn1c/
++    else
++        ./configure --prefix /opt/asn1c/
++    fi
+     make -j`nproc`
+     $SUDO make install
+     cd -
+@@ -587,9 +593,27 @@ install_asn1c_from_source(){
+ 
+ install_simde_from_source(){
+     echo_info "\nInstalling SIMDE from source without test cases (header files only)"
++    if [[ -f /usr/include/simde/simde-common.h ]]; then
++      echo_info "SIMDE headers already installed; skipping download"
++      return 0
++    fi
+     cd /tmp
+     $SUDO rm -rf /tmp/simde
+-    git clone https://github.com/simd-everywhere/simde-no-tests.git /tmp/simde
++    for attempt in 1 2 3; do
++      git -c http.version=HTTP/1.1 clone https://github.com/simd-everywhere/simde-no-tests.git /tmp/simde && break
++      echo_warning "SIMDE download failed (attempt ${attempt}/3)"
++      $SUDO rm -rf /tmp/simde
++    done
++    if [[ ! -d /tmp/simde/.git ]]; then
++      if [[ "$OS_DISTRO" == "ubuntu" ]]; then
++        echo_warning "GitHub unavailable; falling back to Ubuntu libsimde-dev"
++        $SUDO apt-get update
++        $SUDO apt-get install -y libsimde-dev
++        return $?
++      fi
++      echo_error "Unable to install SIMDE headers"
++      return 1
++    fi
+     cd /tmp/simde
+     # we can specify a given version of SIMDE (sha-one or tag)
+     if [[ -v SIMDE_VERSION ]]; then
+diff --git a/common/utils/LOG/log.c b/common/utils/LOG/log.c
+index 1fe2280c15..17c757dddc 100644
+--- a/common/utils/LOG/log.c
++++ b/common/utils/LOG/log.c
+@@ -61,7 +61,7 @@
+ 
+ // Fixme: a better place to be shure it is called 
+ void read_cpu_hardware (void) __attribute__ ((constructor));
+-#if !defined(__arm__) && !defined(__aarch64__) 
++#if defined(__i386__) || defined(__x86_64__)
+   void read_cpu_hardware (void) {__builtin_cpu_init(); }
+ #else 
+   void read_cpu_hardware (void) {}
 diff --git a/common/utils/actor/actor.h b/common/utils/actor/actor.h
 index 023533d2d7..c6be9b1d1f 100644
 --- a/common/utils/actor/actor.h
@@ -79,6 +172,19 @@ index 825012ce6a..c8f8834b76 100644
        if (!farray[i].fptr) {
          fprintf(stderr, "[LOADER] load_module_shlib(): function %s not found: %s\n",
                    farray[i].fname, dlerror());
+diff --git a/common/utils/threadPool/task_ans.h b/common/utils/threadPool/task_ans.h
+index 2fa7d0fc83..603c6cf916 100644
+--- a/common/utils/threadPool/task_ans.h
++++ b/common/utils/threadPool/task_ans.h
+@@ -43,7 +43,7 @@ extern "C" {
+ 
+ #if defined(__i386__) || defined(__x86_64__)
+ #define LEVEL1_DCACHE_LINESIZE 64
+-#elif defined(__aarch64__)
++#elif defined(__aarch64__) || defined(__riscv)
+ // This is not always true for ARM
+ // in linux, you can obtain the size at runtime using sysconf (_SC_LEVEL1_DCACHE_LINESIZE)
+ // or from the bash with the command $ getconf LEVEL1_DCACHE_LINESIZE
 diff --git a/common/utils/threadPool/thread-pool.c b/common/utils/threadPool/thread-pool.c
 index 2a31602e6e..95191d3d11 100644
 --- a/common/utils/threadPool/thread-pool.c
@@ -154,6 +260,42 @@ index 2a31602e6e..95191d3d11 100644
    free(args);
    return NULL;
  }
+diff --git a/common/utils/time_meas.h b/common/utils/time_meas.h
+index de79a0252d..9fc2224194 100644
+--- a/common/utils/time_meas.h
++++ b/common/utils/time_meas.h
+@@ -35,7 +35,7 @@
+ extern int cpu_meas_enabled;
+ extern double cpu_freq_GHz  __attribute__ ((aligned(32)));;
+ // structure to store data to compute cpu measurment
+-#if defined(__x86_64__) || defined(__i386__) || defined(__arm__) || defined(__aarch64__)
++#if defined(__x86_64__) || defined(__i386__) || defined(__arm__) || defined(__aarch64__) || defined(__riscv)
+   typedef long long oai_cputime_t;
+ #else
+   #error "building on unsupported CPU architecture"
+@@ -100,6 +100,22 @@ static inline unsigned long long rdtsc_oai(void) {
+   __asm__ volatile ("rdtsc" : "=a" (a), "=d" (d));
+   return (d<<32) | a;
+ }
++#elif defined(__riscv)
++/*
++ * Linux may disable user-space access to the RISC-V cycle counter.  Using
++ * rdcycle then raises SIGILL on K3, so use the kernel-provided monotonic clock.
++ * The returned unit is nanoseconds; get_cpu_freq_GHz() consequently evaluates
++ * to 1.0 and the existing cycle-to-time formulas remain valid.
++ */
++static inline uint64_t rdtsc_oai(void) __attribute__((always_inline));
++static inline uint64_t rdtsc_oai(void)
++{
++  struct timespec ts;
++  if (clock_gettime(CLOCK_MONOTONIC_RAW, &ts) != 0)
++    return 0;
++  return (uint64_t)ts.tv_sec * 1000000000ULL + (uint64_t)ts.tv_nsec;
++}
++
+ #elif defined(__aarch64__)
+ static inline uint64_t rdtsc_oai(void) __attribute__((always_inline));
+ static inline uint64_t rdtsc_oai(void)
 diff --git a/detect_host.sh b/detect_host.sh
 new file mode 100755
 index 0000000000..92e06a28e2
@@ -1443,6 +1585,33 @@ index 3fece46ac8..826298acd2 100644
    for (int pusch_id = 0; pusch_id < nrLDPC_slot_decoding_parameters->nb_TBs; pusch_id++) {
      nrLDPC_TB_decoding_parameters_t *nrLDPC_TB_decoding_parameters = &nrLDPC_slot_decoding_parameters->TBs[pusch_id];
      for (int r = 0; r < nrLDPC_TB_decoding_parameters->C; r++) {
+diff --git a/openair1/PHY/CODING/nrLDPC_coding/nrLDPC_coding_segment/nrLDPC_coding_segment_encoder.c b/openair1/PHY/CODING/nrLDPC_coding/nrLDPC_coding_segment/nrLDPC_coding_segment_encoder.c
+index 5d0e90e46b..07b977372f 100644
+--- a/openair1/PHY/CODING/nrLDPC_coding/nrLDPC_coding_segment/nrLDPC_coding_segment_encoder.c
++++ b/openair1/PHY/CODING/nrLDPC_coding/nrLDPC_coding_segment/nrLDPC_coding_segment_encoder.c
+@@ -157,6 +157,22 @@ static void write_task_output(uint8_t *f,
+     output_p++;
+   }
+        
++#elif defined(__riscv)
++  // Portable scalar fallback. Each input byte contains one bit from up to
++  // eight segments; write each segment contiguously into the packed output.
++  uint32_t segment_offset = Eoffset;
++  for (uint32_t j = 0; j < nb_segments; j++) {
++    const bool first_group = j < E2_first_segment;
++    const uint8_t *src = first_group ? f : f2;
++    const uint32_t segment_bits = first_group ? E : E2;
++    for (uint32_t i = 0; i < segment_bits; i++) {
++      const uint8_t bit = (src[i] >> j) & 1U;
++      const uint32_t output_bit = segment_offset + i;
++      output[output_bit >> 3] |= bit << (output_bit & 7);
++    }
++    segment_offset += segment_bits;
++  }
++
+ #else
+   uint32_t *output_p = (uint32_t*)output;
+ 
 diff --git a/openair1/PHY/CODING/nrLDPC_decoder/nrLDPC_decoder.c b/openair1/PHY/CODING/nrLDPC_decoder/nrLDPC_decoder.c
 index d3b9a58da1..4e369950e7 100644
 --- a/openair1/PHY/CODING/nrLDPC_decoder/nrLDPC_decoder.c
@@ -2057,6 +2226,19 @@ index 3f4660744d..bd4f56baf6 100644
        // Calculation of a group of two terms in the bit metric involving product of psi and interference
        psi_a_s[j] = prodsum_psi_a_epi16_256(psi_r_s[j], a_r_s[j], psi_i_s[j], a_i_s[j]);
  
+diff --git a/openair1/PHY/TOOLS/oai_dfts.c b/openair1/PHY/TOOLS/oai_dfts.c
+index 1a86bfdc27..ed96183e99 100644
+--- a/openair1/PHY/TOOLS/oai_dfts.c
++++ b/openair1/PHY/TOOLS/oai_dfts.c
+@@ -19,7 +19,7 @@
+  *      contact@openairinterface.org
+  */
+ 
+-#if defined(__x86_64__) || defined(__i386__)
++#if defined(__x86_64__) || defined(__i386__) || defined(__riscv)
+ #include <stdio.h>
+ #include <stdlib.h>
+ #include <string.h>
 diff --git a/openair1/PHY/defs_RU.h b/openair1/PHY/defs_RU.h
 index e72465c4a9..0601d728d0 100644
 --- a/openair1/PHY/defs_RU.h
diff --git a/plugins/CMakeLists.txt b/plugins/CMakeLists.txt
index 3b89926..45cb008 100644
--- a/plugins/CMakeLists.txt
+++ b/plugins/CMakeLists.txt
@@ -7,39 +7,46 @@
 
 set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR})
 
-option(ENABLE_CUDA "Enable CUDA package" ON)
+option(ENABLE_CUDA "Build NVIDIA CUDA/TensorRT plugins" OFF)
+
+# RISC-V targets in this port are CPU-only. Keep this guard here as a second
+# line of defence in case a caller accidentally passes -DENABLE_CUDA=ON.
+if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(riscv64|riscv)$")
+  if(ENABLE_CUDA)
+    message(WARNING "CUDA was requested on RISC-V; forcing ENABLE_CUDA=OFF")
+  endif()
+  set(ENABLE_CUDA OFF CACHE BOOL "Build NVIDIA CUDA/TensorRT plugins" FORCE)
+  set(ENABLE_DGX_OPTIMIZATIONS OFF CACHE BOOL "Use DGX-specific fast paths" FORCE)
+endif()
 
 if (ENABLE_CUDA)
   find_package(CUDAToolkit REQUIRED)
   enable_language(CUDA)
-  # Support multiple CUDA architectures: Orin (sm_87), Thor (sm_110), DGX Spark (sm_121)
-  # Can be overridden via -DCMAKE_CUDA_ARCHITECTURES=<arch>; plugins may set CUDA_ARCHITECTURES per target
   if(NOT DEFINED CMAKE_CUDA_ARCHITECTURES)
     set(CMAKE_CUDA_ARCHITECTURES "87;110;121")
   endif()
 endif()
 
-# Base plugin entry points (subdirectories add to this via PARENT_SCOPE)
 set(PLUGINS_SRC
     common/src/plugins.c
 )
 
-# Add plugin subdirectories
 if (ENABLE_CUDA)
   add_subdirectory(neural_demapper)
   add_subdirectory(neural_receiver)
   add_subdirectory(ldpc_cuda)
   add_subdirectory(channel_emulation)
-endif ()
+else()
+  message(STATUS "CPU-only plugin build: CUDA LDPC, TensorRT neural plugins, and CUDA channel emulation are disabled")
+  list(APPEND PLUGINS_SRC common/src/cpu_only_stubs.c)
+endif()
 add_subdirectory(data_acquisition)
 add_subdirectory(link_adaptation)
 
-# ASN.1 include paths needed by MAC plugin headers (link_adaptation)
 include_directories(${CMAKE_BINARY_DIR}/openair2/RRC/NR/MESSAGES)
 include_directories(${CMAKE_BINARY_DIR}/openair2/RRC/LTE/MESSAGES)
 include_directories(${CMAKE_BINARY_DIR})
 
-# Build plugins plugin library with all sources
 add_library(plugins_plugins STATIC ${PLUGINS_SRC} common/src/mac_plugins.c ${MAC_PLUGINS_SRC})
 add_dependencies(plugins_plugins generate_T asn1_nr_rrc_hdrs gen_nr_rrc_hdrs asn1_lte_rrc_hdrs gen_lte_rrc_hdrs)
 target_link_libraries(nr-uesoftmodem PUBLIC plugins_plugins)
diff --git a/plugins/channel_emulation/tutorial.yaml b/plugins/channel_emulation/tutorial.yaml
index ac092ed..be470c8 100644
--- a/plugins/channel_emulation/tutorial.yaml
+++ b/plugins/channel_emulation/tutorial.yaml
@@ -8,13 +8,13 @@ description: CUDA channel emulator with file-based and ZMQ CIR sources
 
 tests:
   unit:
-    enabled: true
+    enabled: false # CUDA emulator disabled for the K3 CPU baseline
     command: |
       pytest cuda_emulator/tests/unit -v
       pytest file_loader/tests/unit -v
       pytest zmq_loader/tests/unit -v
   integration:
-    enabled: true
+    enabled: false # CUDA emulator disabled for the K3 CPU baseline
     command: "tests/integration/run_integration.sh"
 
 build:
diff --git a/plugins/common/build_all_plugins.sh b/plugins/common/build_all_plugins.sh
index 3445cd2..7e254b0 100755
--- a/plugins/common/build_all_plugins.sh
+++ b/plugins/common/build_all_plugins.sh
@@ -36,6 +36,10 @@ done
 # Export so it's available to build scripts
 export BUILD_TARGET
 
+# K3/RV64 uses the CPU-only baseline by default.
+CPU_ONLY="${SIONNA_RK_CPU_ONLY:-1}"
+GPU_ONLY_PLUGINS=" ldpc_cuda neural_demapper neural_receiver channel_emulation "
+
 # Get the absolute path of the script and plugins directory
 SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 PLUGINS_DIR="$(dirname "$SCRIPT_DIR")"
@@ -54,6 +58,12 @@ find "$PLUGINS_DIR" -maxdepth 2 -name "tutorial.yaml" | while read config_file;
 
     echo "Processing plugin: $plugin_name"
 
+    if [ "$CPU_ONLY" = "1" ] && [[ "$GPU_ONLY_PLUGINS" == *" $plugin_name "* ]]; then
+        echo "  Build skipped: GPU-only plugin is disabled in CPU-only mode"
+        echo "----------------------------------------"
+        continue
+    fi
+
     # Parse YAML using python to get build configuration
     # We output shell variable assignments to be evaluated
     eval $(python3 -c "
diff --git a/plugins/common/src/cpu_only_stubs.c b/plugins/common/src/cpu_only_stubs.c
new file mode 100644
index 0000000..4ce4ac2
--- /dev/null
+++ b/plugins/common/src/cpu_only_stubs.c
@@ -0,0 +1,25 @@
+/* CPU-only implementations for plugins that require CUDA or TensorRT. */
+#include "plugins.h"
+#include <stddef.h>
+#include <string.h>
+
+int load_receiver_lib(char *version, receiver_interface_t *interface)
+{
+  (void)version;
+  if (interface != NULL)
+    memset(interface, 0, sizeof(*interface));
+  return 0;
+}
+
+int free_receiver_lib(receiver_interface_t *interface)
+{
+  if (interface != NULL)
+    memset(interface, 0, sizeof(*interface));
+  return 0;
+}
+
+void init_channel_emulator_libs(const NR_DL_FRAME_PARMS *fp) { (void)fp; }
+void init_channel_emulator_worker_thread(void) {}
+void free_channel_emulator_libs(void) {}
+int is_channel_emulation_enabled(void) { return 0; }
+const void *channel_emulator_cir_read_and_apply(void) { return NULL; }
diff --git a/plugins/common/src/plugins.c b/plugins/common/src/plugins.c
index 7c01a6d..4e02087 100644
--- a/plugins/common/src/plugins.c
+++ b/plugins/common/src/plugins.c
@@ -30,8 +30,10 @@ void free_plugins()
 {
     // insert your plugin release/free here.
 
-    free_demapper_lib(&demapper_interface);
-    free_receiver_lib(&receiver_interface);
+    if (demapper_interface.shutdown)
+        free_demapper_lib(&demapper_interface);
+    if (receiver_interface.shutdown)
+        free_receiver_lib(&receiver_interface);
     free_channel_emulator_libs();
 }
 
diff --git a/plugins/ldpc_cuda/tutorial.yaml b/plugins/ldpc_cuda/tutorial.yaml
index 8e2f97e..39cbd0b 100644
--- a/plugins/ldpc_cuda/tutorial.yaml
+++ b/plugins/ldpc_cuda/tutorial.yaml
@@ -8,10 +8,10 @@ description: GPU-accelerated LDPC encoding and decoding
 
 tests:
   unit:
-    enabled: true
+    enabled: false # GPU-only; disabled for the K3 CPU baseline
     command: pytest tests/unit -v
   integration:
-    enabled: true
+    enabled: false # GPU-only; disabled for the K3 CPU baseline
     command: ./tests/integration/run_integration.sh
 
 build:
diff --git a/plugins/neural_demapper/tutorial.yaml b/plugins/neural_demapper/tutorial.yaml
index ce0cc4d..d217cb7 100644
--- a/plugins/neural_demapper/tutorial.yaml
+++ b/plugins/neural_demapper/tutorial.yaml
@@ -8,16 +8,16 @@ description: Neural network-based demapper with TensorRT acceleration
 
 tests:
   unit:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: pytest tests/unit -v
   integration:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: ./tests/integration/run_integration.sh
 
 build:
   host:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: ./scripts/build-trt-plans.sh --plan ./models/neural_demapper.2xfloat16.host.plan
   container:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: docker run --runtime=nvidia -v ../../plugins/:/mnt/plugins --rm -t ran-build-cuda:latest /mnt/plugins/neural_demapper/scripts/build-trt-plans.sh --plan /mnt/plugins/neural_demapper/models/neural_demapper.2xfloat16.plan
diff --git a/plugins/neural_receiver/tutorial.yaml b/plugins/neural_receiver/tutorial.yaml
index 74b1cbf..87eeba6 100644
--- a/plugins/neural_receiver/tutorial.yaml
+++ b/plugins/neural_receiver/tutorial.yaml
@@ -8,16 +8,16 @@ description: End-to-end neural receiver implementation
 
 tests:
   unit:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: pytest tests/unit -v
   integration:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: ./tests/integration/run_integration.sh
 
 build:
   host:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: ./scripts/build-trt-plans.sh --plan ./models/nrx_oai.host.plan
   container:
-    enabled: true
+    enabled: false # TensorRT/CUDA disabled for the K3 CPU baseline
     command: docker run --runtime=nvidia -v ../../plugins/:/mnt/plugins -v ../../ext/neural_rx:/mnt/neural_rx --rm -t ran-build-cuda:latest /mnt/plugins/neural_receiver/scripts/build-trt-plans.sh --onnx /mnt/neural_rx/onnx_models/nrx_oai.onnx --plan /mnt/plugins/neural_receiver/models/nrx_oai.plan
\ No newline at end of file
diff --git a/scripts/build-oai-images.sh b/scripts/build-oai-images.sh
index 4a3998c..7ad7720 100755
--- a/scripts/build-oai-images.sh
+++ b/scripts/build-oai-images.sh
@@ -4,6 +4,12 @@
 # SPDX-License-Identifier: Apache-2.0
 #
 
+if [ "${SIONNA_RK_CPU_ONLY:-1}" = "1" ]; then
+    echo "NVIDIA CUDA image build is disabled in CPU-only mode." >&2
+    echo "Use ./scripts/build-oai-native.sh instead." >&2
+    exit 2
+fi
+
 # functions
 function usage() {
     echo "Usage: $0 [-h|--help] [--debug] [--tag <tagname>] [--no-cache] [--ci] [--force-platform <platform>] <openairinterface5g_dir>"
diff --git a/scripts/configure-system.sh b/scripts/configure-system.sh
index 35411e0..18b23e1 100755
--- a/scripts/configure-system.sh
+++ b/scripts/configure-system.sh
@@ -7,7 +7,7 @@
 # functions
 function usage() {
     echo "Usage: $0 [-h|--help] [--force-platform <platform>] [--verbose] [--dry-run]"
-    echo "Platforms: agx-orin, agx-thor, orin-nano, dgx-spark"
+    echo "Platforms: k3, agx-orin, agx-thor, orin-nano, dgx-spark"
     exit 1
 }
 
@@ -62,6 +62,12 @@ echo "Platform: $platform"
 echo "Model: $model"
 echo "Family: $family"
 
+if [ "$platform" == "k3" ] || ([ "$(uname -m)" == "riscv64" ] && [ "$platform" == "unknown" ]); then
+    echo "Configuring system as 'K3 Pico-ITX' in CPU-only mode"
+    execute "${source_dir}/scripts/configure-system.k3.sh"
+    exit 0
+fi
+
 # select the script according to platform
 if [ "$platform" == "agx-orin" ] || ([ "$model" == "NVIDIA Jetson AGX Orin Developer Kit" ] && [ "$platform" == "unknown" ]); then
     echo "Configuring system as 'Jetson AGX Orin Developer Kit'"
diff --git a/scripts/detect_host.sh b/scripts/detect_host.sh
index 73aa254..a0f5a6c 100755
--- a/scripts/detect_host.sh
+++ b/scripts/detect_host.sh
@@ -7,6 +7,12 @@
 # default values
 family="Unknown"
 model="Unknown"
+arch=$(uname -m)
+
+if [ "$arch" == "riscv64" ]; then
+    echo "K3 Pico-ITX"
+    exit 0
+fi
 
 if [ -f "/sys/devices/virtual/dmi/id/product_family" ]; then
     family=$(cat /sys/devices/virtual/dmi/id/product_family)
diff --git a/scripts/quickstart-oai.sh b/scripts/quickstart-oai.sh
index 7874ab4..1358a7e 100755
--- a/scripts/quickstart-oai.sh
+++ b/scripts/quickstart-oai.sh
@@ -10,7 +10,7 @@ check-license
 
 # functions
 function usage() {
-    echo "Usage: $0 [-h|--help] [--clean] [--debug] [--no-build] [--tag <tagname>] [--ci] [--oai-version <oai-version>] --source <kit-rootdir> --dest <openairinterface5g_dir>"
+    echo "Usage: $0 [-h|--help] [--clean] [--debug] [--no-build] [--install-deps] [--enable-gpu] [--tag <tagname>] [--ci] [--oai-version <oai-version>] --source <kit-rootdir> --dest <openairinterface5g_dir>"
     exit 1
 }
 
@@ -36,6 +36,8 @@ no_build=0
 no_tutorials=0
 debug=0
 ci=0
+install_deps=0
+cpu_only=${SIONNA_RK_CPU_ONLY:-1}
 
 # Parse command-line arguments
 while [[ $# -gt 0 ]]; do
@@ -47,6 +49,8 @@ while [[ $# -gt 0 ]]; do
         --oai-version) OAI_VERSION="$2"; shift ;;
         --clean) clean_dest=1 ;;
         --no-build) no_build=1 ;;
+        --install-deps) install_deps=1 ;;
+        --enable-gpu) cpu_only=0 ;;
         --debug) debug=1 ;;
         --ci) ci=1 ;;
         *) usage; exit 1 ;;
@@ -65,7 +69,8 @@ dest_dir=$(realpath -sm "$dest_dir")
 
 echo "source: $source_dir"
 echo "dest: $dest_dir"
-echo "arch: $arch"
+echo "arch: $(uname -m)"
+echo "CPU-only: $cpu_only"
 
 # If clean install, remove destination directory
 if [ "$clean_dest" = "1" ] && [ -d "$dest_dir" ]; then
@@ -106,14 +111,27 @@ pushd "$dest_dir"
 git submodule update --init --recursive
 popd
 
+if [ "$cpu_only" = "1" ] && [ ! -e "${dest_dir}/plugins" ]; then
+    ln -s "${source_dir}/plugins" "${dest_dir}/plugins"
+fi
+
 if [ "$no_build" = "0" ]; then
-    echo "Build OAI images..."
-    extra_opts=""
-    if [ "$debug" = "1" ]; then
-        extra_opts="$extra_opts --debug"
-    fi
-    if [ "$ci" = "1" ]; then
-        extra_opts="$extra_opts --ci"
+    if [ "$cpu_only" = "1" ]; then
+        echo "Build OAI natively in CPU-only mode..."
+        native_opts=""
+        if [ "$install_deps" = "1" ]; then
+            native_opts="--install-deps"
+        fi
+        SIONNA_RK_CPU_ONLY=1 "${source_dir}/scripts/build-oai-native.sh" $native_opts "$dest_dir"
+    else
+        echo "Build NVIDIA CUDA images..."
+        extra_opts=""
+        if [ "$debug" = "1" ]; then
+            extra_opts="$extra_opts --debug"
+        fi
+        if [ "$ci" = "1" ]; then
+            extra_opts="$extra_opts --ci"
+        fi
+        SIONNA_RK_CPU_ONLY=0 "${source_dir}/scripts/build-oai-images.sh" $extra_opts --tag "$TAG" "$dest_dir"
     fi
-    "${source_dir}/scripts/build-oai-images.sh" $extra_opts --tag "$TAG" "$dest_dir"
 fi
diff --git a/scripts/start_channel_emulation_demo.sh b/scripts/start_channel_emulation_demo.sh
index 26e3aa4..e6b2909 100755
--- a/scripts/start_channel_emulation_demo.sh
+++ b/scripts/start_channel_emulation_demo.sh
@@ -1,4 +1,10 @@
 #!/bin/bash
+
+if [ "${SIONNA_RK_CPU_ONLY:-1}" = "1" ]; then
+    echo "CUDA channel emulation demo is disabled in CPU-only mode." >&2
+    exit 2
+fi
+
 #
 # SPDX-FileCopyrightText: Copyright (c) 2024-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 # SPDX-License-Identifier: Apache-2.0
diff --git a/scripts/start_mps.sh b/scripts/start_mps.sh
index 4456d06..4d7a724 100755
--- a/scripts/start_mps.sh
+++ b/scripts/start_mps.sh
@@ -1,5 +1,11 @@
 #!/bin/bash
 
+if [ "${SIONNA_RK_CPU_ONLY:-1}" = "1" ]; then
+    echo "NVIDIA MPS is disabled in CPU-only mode."
+    exit 0
+fi
+
+
 export CUDA_MPS_PIPE_DIRECTORY=/tmp/nvidia-mps
 export CUDA_MPS_LOG_DIRECTORY=/tmp/nvidia-log
 
diff --git a/scripts/stop_mps.sh b/scripts/stop_mps.sh
index dae2608..c48573e 100755
--- a/scripts/stop_mps.sh
+++ b/scripts/stop_mps.sh
@@ -1,4 +1,10 @@
 #!/bin/bash
 
+if [ "${SIONNA_RK_CPU_ONLY:-1}" = "1" ]; then
+    echo "NVIDIA MPS is disabled in CPU-only mode."
+    exit 0
+fi
+
+
 echo "Stopping MPS Server"
 echo "quit" | nvidia-cuda-mps-control
diff --git a/requirements_cpu.txt b/requirements_cpu.txt
new file mode 100644
index 0000000..27e34b5
--- /dev/null
+++ b/requirements_cpu.txt
@@ -0,0 +1,10 @@
+# Python dependencies for the K3/RV64 CPU-only baseline.
+# NVIDIA TensorRT and nvidia-modelopt are intentionally excluded.
+numpy
+matplotlib
+nanobind
+onnx
+onnxscript
+pytest
+pyzmq
+pyyaml
diff --git a/scripts/build-oai-native.sh b/scripts/build-oai-native.sh
new file mode 100755
index 0000000..a819814
--- /dev/null
+++ b/scripts/build-oai-native.sh
@@ -0,0 +1,68 @@
+#!/bin/bash
+# Build the patched OpenAirInterface gNB and nrUE natively without NVIDIA GPU dependencies.
+
+set -euo pipefail
+
+usage() {
+    echo "Usage: $0 [--install-deps] [openairinterface5g_dir]"
+}
+
+install_deps=0
+oai_dir=""
+
+while [[ $# -gt 0 ]]; do
+    case "$1" in
+        --install-deps)
+            install_deps=1
+            shift
+            ;;
+        -h|--help)
+            usage
+            exit 0
+            ;;
+        *)
+            if [ -n "$oai_dir" ]; then
+                usage >&2
+                exit 1
+            fi
+            oai_dir="$1"
+            shift
+            ;;
+    esac
+done
+
+project_root=$(realpath "$(dirname "${BASH_SOURCE[0]}")/..")
+oai_dir=$(realpath -sm "${oai_dir:-${project_root}/ext/openairinterface5g}")
+
+if [ ! -x "${oai_dir}/cmake_targets/build_oai" ]; then
+    echo "Error: OpenAirInterface source is missing at ${oai_dir}." >&2
+    echo "Run ./scripts/quickstart-oai.sh --no-build first." >&2
+    exit 1
+fi
+
+if [ ! -e "${oai_dir}/plugins" ]; then
+    ln -s "${project_root}/plugins" "${oai_dir}/plugins"
+fi
+
+export SIONNA_RK_CPU_ONLY=1
+export CUDA_VISIBLE_DEVICES=""
+export NVIDIA_VISIBLE_DEVICES=void
+
+build_args=(
+    -w SIMU
+    --gNB
+    --nrUE
+    --cmake-opt -DENABLE_PLUGINS=ON
+    --cmake-opt -DENABLE_CUDA=OFF
+    --cmake-opt -DENABLE_DGX_OPTIMIZATIONS=OFF
+    --cmake-opt -DAVX2=OFF
+    --cmake-opt -DAVX512=OFF
+)
+
+if [ "$install_deps" = "1" ]; then
+    build_args=(-I "${build_args[@]}")
+fi
+
+echo "Building OpenAirInterface natively in CPU-only mode"
+echo "OAI source: ${oai_dir}"
+"${oai_dir}/cmake_targets/build_oai" "${build_args[@]}"
diff --git a/scripts/configure-system.k3.sh b/scripts/configure-system.k3.sh
new file mode 100755
index 0000000..862d9f5
--- /dev/null
+++ b/scripts/configure-system.k3.sh
@@ -0,0 +1,29 @@
+#!/bin/bash
+# Configure the SpacemiT K3 Pico-ITX for the Sionna-RK CPU-only port.
+
+set -euo pipefail
+
+if [ "$(uname -m)" != "riscv64" ]; then
+    echo "Error: this configuration is intended for a riscv64 host." >&2
+    exit 1
+fi
+
+export SIONNA_RK_CPU_ONLY=1
+export CUDA_VISIBLE_DEVICES=""
+export NVIDIA_VISIBLE_DEVICES=void
+
+sudo apt-get update
+sudo apt-get install -y \
+    build-essential \
+    cmake \
+    git \
+    ninja-build \
+    pkg-config \
+    python3 \
+    python3-pip \
+    python3-venv \
+    libcjson-dev \
+    libzmq3-dev
+
+echo "K3 CPU-only prerequisites installed."
+echo "CUDA, TensorRT, NVIDIA Container Runtime, and L4T packages were not installed."
diff --git a/scripts/k3-rfsim-message-test.sh b/scripts/k3-rfsim-message-test.sh
new file mode 100755
index 0000000..e2f0761
--- /dev/null
+++ b/scripts/k3-rfsim-message-test.sh
@@ -0,0 +1,65 @@
+#!/usr/bin/env bash
+set -euo pipefail
+GNB_UNIT=k3-rfsim-gnb.service
+UE_UNIT=k3-rfsim-nrue.service
+for unit in "$GNB_UNIT" "$UE_UNIT"; do
+  systemctl is-active --quiet "$unit" || { echo "ERROR: $unit is not active." >&2; exit 1; }
+done
+for dev in oaitun_enb1 oaitun_ue1; do
+  ip link show "$dev" >/dev/null 2>&1 || { echo "ERROR: interface $dev is missing." >&2; exit 1; }
+done
+old_all_rpf=$(sysctl -n net.ipv4.conf.all.rp_filter)
+old_enb_rpf=$(sysctl -n net.ipv4.conf.oaitun_enb1.rp_filter)
+old_ue_rpf=$(sysctl -n net.ipv4.conf.oaitun_ue1.rp_filter)
+old_all_local=$(sysctl -n net.ipv4.conf.all.accept_local)
+old_enb_local=$(sysctl -n net.ipv4.conf.oaitun_enb1.accept_local)
+old_ue_local=$(sysctl -n net.ipv4.conf.oaitun_ue1.accept_local)
+restore() {
+  sudo sysctl -q -w net.ipv4.conf.all.rp_filter="$old_all_rpf"     net.ipv4.conf.oaitun_enb1.rp_filter="$old_enb_rpf"     net.ipv4.conf.oaitun_ue1.rp_filter="$old_ue_rpf"     net.ipv4.conf.all.accept_local="$old_all_local"     net.ipv4.conf.oaitun_enb1.accept_local="$old_enb_local"     net.ipv4.conf.oaitun_ue1.accept_local="$old_ue_local" >/dev/null 2>&1 || true
+}
+trap restore EXIT INT TERM
+sudo sysctl -q -w net.ipv4.conf.all.rp_filter=0   net.ipv4.conf.oaitun_enb1.rp_filter=0   net.ipv4.conf.oaitun_ue1.rp_filter=0   net.ipv4.conf.all.accept_local=1   net.ipv4.conf.oaitun_enb1.accept_local=1   net.ipv4.conf.oaitun_ue1.accept_local=1
+python3 - <<'PY'
+import queue
+import socket
+import threading
+import time
+
+SO_BINDTODEVICE = socket.SO_BINDTODEVICE
+
+def exchange(label, recv_ip, recv_port, send_ip, send_port, send_dev, payload):
+    result = queue.Queue()
+    ready = threading.Event()
+    def receiver():
+        try:
+            with socket.socket(socket.AF_INET, socket.SOCK_DGRAM) as s:
+                s.settimeout(10)
+                s.bind((recv_ip, recv_port))
+                ready.set()
+                data, peer = s.recvfrom(2048)
+                result.put((data, peer, None))
+        except Exception as exc:
+            result.put((None, None, exc))
+    thread = threading.Thread(target=receiver, daemon=True)
+    thread.start()
+    if not ready.wait(2):
+        raise RuntimeError(f"{label}: receiver did not start")
+    time.sleep(0.3)
+    with socket.socket(socket.AF_INET, socket.SOCK_DGRAM) as s:
+        s.setsockopt(socket.SOL_SOCKET, SO_BINDTODEVICE, send_dev.encode() + b"\0")
+        s.bind((send_ip, send_port))
+        sent = s.sendto(payload, (recv_ip, recv_port))
+    thread.join(11)
+    if thread.is_alive():
+        raise RuntimeError(f"{label}: receive timeout")
+    data, peer, error = result.get_nowait()
+    if error:
+        raise error
+    if data != payload:
+        raise RuntimeError(f"{label}: expected {payload!r}, received {data!r}")
+    print(f"{label}: sent {sent} bytes; received {data.decode()} from {peer[0]}:{peer[1]}")
+
+exchange("UE -> gNB", "10.0.1.1", 5001, "10.0.1.2", 5002, "oaitun_ue1", b"hello")
+exchange("gNB -> UE", "10.0.1.2", 5002, "10.0.1.1", 5001, "oaitun_enb1", b"world")
+print("PASS: bidirectional application messages crossed the OAI RFsim data bearer.")
+PY
diff --git a/scripts/run-k3-rfsim.sh b/scripts/run-k3-rfsim.sh
new file mode 100755
index 0000000..db5705d
--- /dev/null
+++ b/scripts/run-k3-rfsim.sh
@@ -0,0 +1,77 @@
+#!/usr/bin/env bash
+set -euo pipefail
+ROOT="${SIONNA_RK_ROOT:-$HOME/sionna-rk}"
+BUILD="$ROOT/ext/openairinterface5g/cmake_targets/ran_build/build"
+GNB_CONF="$ROOT/ext/openairinterface5g/ci-scripts/conf_files/gnb.sa.band78.106prb.rfsim.conf"
+UE_CONF="$ROOT/ext/openairinterface5g/targets/PROJECTS/GENERIC-NR-5GC/CONF/ue.conf"
+GNB_UNIT=k3-rfsim-gnb.service
+UE_UNIT=k3-rfsim-nrue.service
+PORT=4043
+die() { printf 'ERROR: %s\n' "$*" >&2; exit 1; }
+active() { sudo systemctl is-active --quiet "$1"; }
+show_processes() { ps -eo pid,ppid,pgid,sid,stat,comm,args | awk 'NR==1 || /nr-softmodem|nr-uesoftmodem|[g]db .*softmodem|[s]trace .*softmodem/'; }
+preflight_common() {
+  [ "$(uname -m)" = "riscv64" ] || die "This launcher is for RV64 K3; detected $(uname -m)."
+  [ -x "$BUILD/nr-softmodem" ] || die "Missing $BUILD/nr-softmodem; build OAI first."
+  [ -x "$BUILD/nr-uesoftmodem" ] || die "Missing $BUILD/nr-uesoftmodem; build OAI first."
+  [ -r "$GNB_CONF" ] || die "Missing gNB config: $GNB_CONF"
+  [ -r "$UE_CONF" ] || die "Missing UE config: $UE_CONF"
+}
+start_gnb() {
+  preflight_common
+  active "$GNB_UNIT" && die "$GNB_UNIT is already active."
+  pgrep -x nr-softmodem >/dev/null && { show_processes; die "An unmanaged gNB exists. Stop its original terminal process first."; }
+  pgrep -x nr-uesoftmodem >/dev/null && { show_processes; die "An unmanaged nrUE exists. Stop it first."; }
+  ss -ltn "sport = :$PORT" 2>/dev/null | grep -q LISTEN && die "TCP $PORT is occupied."
+  sudo systemctl reset-failed "$GNB_UNIT" 2>/dev/null || true
+  sudo systemd-run --unit="$GNB_UNIT" --service-type=exec --property=Restart=no --property=KillMode=mixed --working-directory="$BUILD" "$BUILD/nr-softmodem" -O "$GNB_CONF" --rfsim --rfsimulator.serveraddr server --phy-test --noS1 --gNBs.[0].min_rxtxtime 6 --T_stdout 1
+  sleep 2
+  active "$GNB_UNIT" || { sudo journalctl -u "$GNB_UNIT" -n 100 --no-pager; die "gNB unit failed."; }
+  echo "gNB is managed by systemd and survives terminal closure."
+  echo "View logs: $0 gnb-log"
+}
+start_ue() {
+  preflight_common
+  active "$UE_UNIT" && die "$UE_UNIT is already active."
+  active "$GNB_UNIT" || die "$GNB_UNIT is not active. Run: $0 gnb-start"
+  ss -ltn "sport = :$PORT" 2>/dev/null | grep -q LISTEN || die "gNB is not listening on TCP $PORT."
+  pgrep -x nr-uesoftmodem >/dev/null && { show_processes; die "An unmanaged nrUE exists. Stop it first."; }
+  sudo systemctl reset-failed "$UE_UNIT" 2>/dev/null || true
+  sudo systemd-run --unit="$UE_UNIT" --service-type=exec --property=Restart=no --property=KillMode=mixed --working-directory="$BUILD" "$BUILD/nr-uesoftmodem" -O "$UE_CONF" --rfsim --rfsimulator.serveraddr 127.0.0.1 --phy-test --noS1 -r 106 --numerology 1 --band 78 -C 3619200000 --ue-rxgain 140 --ue-txgain 0 --T_stdout 1
+  sleep 2
+  active "$UE_UNIT" || { sudo journalctl -u "$UE_UNIT" -n 120 --no-pager; die "nrUE unit failed."; }
+  echo "nrUE is managed by systemd and survives terminal closure."
+  echo "View logs: $0 ue-log"
+}
+case "${1:-}" in
+  check|status)
+    preflight_common
+    echo "Architecture: $(uname -m)"; free -h
+    sudo systemctl --no-pager --full status "$GNB_UNIT" "$UE_UNIT" 2>/dev/null || true
+    echo "RFsimulator TCP port $PORT:"; ss -ltnp "sport = :$PORT" 2>/dev/null || true
+    echo "Relevant processes:"; show_processes
+    ;;
+  gnb-start|gnb) start_gnb ;;
+  ue-start|ue) start_ue ;;
+  gnb-log) exec sudo journalctl -fu "$GNB_UNIT" -n 80 ;;
+  ue-log) exec sudo journalctl -fu "$UE_UNIT" -n 120 ;;
+  message-test) exec "$ROOT/scripts/k3-rfsim-message-test.sh" ;;
+  ue-stop)
+    sudo systemctl stop "$UE_UNIT"
+    echo "nrUE stopped."
+    ;;
+  gnb-stop)
+    active "$UE_UNIT" && die "Stop nrUE first: $0 ue-stop"
+    sudo systemctl stop "$GNB_UNIT"
+    echo "gNB stopped."
+    ;;
+  stop)
+    sudo systemctl stop "$UE_UNIT" 2>/dev/null || true
+    sudo systemctl stop "$GNB_UNIT" 2>/dev/null || true
+    echo "K3 RFsimulator units stopped."
+    ;;
+  *)
+    echo "Usage: $0 check|gnb-start|gnb-log|ue-start|ue-log|message-test|ue-stop|gnb-stop|stop"
+    exit 2
+    ;;
+esac
K3_RV64_PATCH
```

### 8.2 校验补丁

```bash
sha256sum ~/sionna-rk-k3-rv64.patch
```

预期：

```text
b35232cc7ec640be1b7a899fcfb9fd5887f86748376095da06b2883886d55738  /home/ubuntu/sionna-rk-k3-rv64.patch
```

如果用户名不是 `ubuntu`，路径部分会不同，但哈希必须相同。

检查补丁统计：

```bash
cd ~/sionna-rk
git apply --stat ~/sionna-rk-k3-rv64.patch
git apply --check ~/sionna-rk-k3-rv64.patch
```

`git apply --check` 无输出即通过。

### 8.3 应用补丁

```bash
cd ~/sionna-rk
git apply ~/sionna-rk-k3-rv64.patch
```

检查：

```bash
git status --short
test -f scripts/build-oai-native.sh
test -f scripts/configure-system.k3.sh
test -f requirements_cpu.txt
test -f plugins/common/src/cpu_only_stubs.c
test -f patches/openairinterface5g.patch
echo "K3 RV64 files present"
```

补充执行权限：

```bash
chmod +x \
  scripts/build-oai-native.sh \
  scripts/configure-system.k3.sh \
  scripts/detect_host.sh \
  scripts/quickstart-oai.sh
```

检查 Shell 语法：

```bash
bash -n scripts/build-oai-native.sh
bash -n scripts/configure-system.k3.sh
bash -n scripts/quickstart-oai.sh
```

应用完成后直接跳到第 13 节继续安装和构建。

---

## 9. 方式 B：逐文件修改的通用方法

方式 B 使用第 8 节生成的同一补丁作为精确代码来源，但不一次性应用全部内容。

先查看文件列表：

```bash
cd ~/sionna-rk
grep '^diff --git ' ~/sionna-rk-k3-rv64.patch
```

查看某个文件的完整修改：

```bash
git apply --numstat ~/sionna-rk-k3-rv64.patch
less ~/sionna-rk-k3-rv64.patch
```

在 `less` 中输入 `/diff --git a/文件路径` 定位，按 `q` 退出。

逐文件应用示例：

```bash
git apply --include='Makefile' ~/sionna-rk-k3-rv64.patch
git diff -- Makefile
```

确认后继续应用下一文件。也可以用 `nano`/`vim` 依据补丁中的 `-`、`+` 行手工编辑：

```bash
nano Makefile
```

补丁语法：

- `--- a/...` 和 `+++ b/...` 表示修改前后文件；
- 以 `-` 开头的是删除内容；
- 以 `+` 开头的是新增内容；
- 无前缀的是上下文，不能随意省略语义；
- `new file mode` 表示需要新建文件。

每完成一组修改都执行：

```bash
git diff --check
git status --short
```

以下章节说明每组文件为什么修改。

---

## 10. Sionna-RK 根仓库修改及原因

### 10.1 `Makefile`

**目的**：让顶层入口识别 `SIONNA_RK_CPU_ONLY=1`，在 RV64 上不进入 NVIDIA Docker/GPU 流程，改为调用原生 OAI 构建脚本。

检查：

```bash
grep -n 'SIONNA_RK_CPU_ONLY' Makefile
git diff -- Makefile
```

### 10.2 `scripts/detect_host.sh`

**目的**：官方检测逻辑以 NVIDIA/DGX/Jetson 为中心；K3 需要识别 `uname -m = riscv64`，并选择 K3 CPU-only 配置。

检查：

```bash
grep -nE 'riscv|k3|CPU_ONLY' scripts/detect_host.sh
```

### 10.3 `scripts/configure-system.sh` 与 `scripts/configure-system.k3.sh`

**目的**：避免在 K3 安装 CUDA、TensorRT、NVIDIA Runtime 和 L4T。新增的 K3 脚本只安装通用编译工具、CJSON 和 ZeroMQ。

检查：

```bash
cat scripts/configure-system.k3.sh
grep -nE 'riscv|k3|CPU_ONLY' scripts/configure-system.sh
```

### 10.4 `scripts/build-oai-native.sh`

**目的**：官方主要通过 NVIDIA CUDA 镜像构建。本文件新增 OAI 原生 CPU 构建入口，强制：

```text
-DENABLE_CUDA=OFF
-DENABLE_DGX_OPTIMIZATIONS=OFF
-DAVX2=OFF
-DAVX512=OFF
```

同时构建 `--gNB`、`--nrUE` 和 `-w SIMU`。

检查：

```bash
cat scripts/build-oai-native.sh
```

### 10.5 `scripts/quickstart-oai.sh`

**目的**：默认 CPU-only；下载 OAI `2025.w34` 后先应用 RV64 补丁，再初始化子模块；CPU 模式建立 `plugins` 符号链接，并调用原生构建脚本。只有显式 `--enable-gpu` 才允许旧 GPU 路径。

检查：

```bash
grep -nE 'CPU-only|enable-gpu|build-oai-native|plugins' scripts/quickstart-oai.sh
```

### 10.6 NVIDIA 专用脚本

涉及：

```text
scripts/build-oai-images.sh
scripts/start_channel_emulation_demo.sh
scripts/start_mps.sh
scripts/stop_mps.sh
```

**目的**：CPU-only 模式下快速退出或拒绝运行，避免误触发 NVIDIA 镜像、MPS 或 GPU 演示。

检查：

```bash
grep -n 'SIONNA_RK_CPU_ONLY' \
  scripts/build-oai-images.sh \
  scripts/start_channel_emulation_demo.sh \
  scripts/start_mps.sh \
  scripts/stop_mps.sh
```

### 10.7 `requirements_cpu.txt`

**目的**：提供不包含 TensorRT 和 `nvidia-modelopt` 的 Python 依赖基线。OAI 原生构建不强制要求安装该文件。

检查：

```bash
cat requirements_cpu.txt
```

---

## 11. 插件层修改及原因

### 11.1 `plugins/CMakeLists.txt`

**目的**：在 CPU-only 下不查找 CUDA/TensorRT，不编译 GPU LDPC、神经接收机、神经解调器等实现；保留 OAI 所需的插件接口和 CPU 实现。

### 11.2 `plugins/common/src/cpu_only_stubs.c`

**目的**：OAI 主程序仍会引用部分插件装载符号。完全删除 GPU 目标会导致最终链接失败，因此提供明确返回“不可用”的 CPU-only 空实现，而不是伪装 GPU 功能可用。

检查：

```bash
sed -n '1,240p' plugins/common/src/cpu_only_stubs.c
grep -n 'cpu_only_stubs' plugins/CMakeLists.txt
```

### 11.3 `plugins/common/build_all_plugins.sh`

**目的**：CPU-only 模式下跳过 CUDA 插件，只构建通用/CPU插件。

### 11.4 各插件 `tutorial.yaml`

涉及：

```text
plugins/channel_emulation/tutorial.yaml
plugins/ldpc_cuda/tutorial.yaml
plugins/neural_demapper/tutorial.yaml
plugins/neural_receiver/tutorial.yaml
```

**目的**：去除或屏蔽 GPU 自动启用项，防止教程配置再次打开 CUDA/TensorRT 插件。

### 11.5 RFsim 配置补丁

涉及：

```text
patches/config/config.b200.patch
patches/config/config.common.patch
patches/config/config.rfsim.patch
patches/config/config.testing.patch
```

**目的**：CPU-only 情况下使用原始/CPU LDPC 和解调器配置，并保证 RFsim 测试不请求 NVIDIA 插件。

---

## 12. OAI RV64 补丁修改及原因

Sionna-RK 根仓库的 `patches/openairinterface5g.patch` 会在下载 OAI 后自动应用。以下是关键修改分类。

### 12.1 Ubuntu 26.04 依赖安装白名单

文件：

```text
cmake_targets/tools/build_helper
```

**原因**：原版 OAI 不接受 Ubuntu 26.04，会在安装依赖前直接退出。补丁增加该发行版，并保持依赖包检测。

### 12.2 CMake 接受 `riscv64`

文件：

```text
CMakeLists.txt
cmake_targets/build_oai
```

**原因**：原版只允许既定 CPU 架构。补丁增加 RV64 分支、关闭 AVX2/AVX512/DGX/CUDA，并避免强制 x86 编译选项。

### 12.3 公共架构分支

涉及：

```text
common/utils/threadPool/task_ans.h
common/utils/time_meas.h
common/utils/LOG/log.c
common/utils/actor/actor.h
common/utils/threadPool/thread-pool.c
```

**原因**：补齐 RISC-V 原子操作、日志和线程相关分支，避免使用 x86-only builtin/汇编。

### 12.4 `rdcycle` 非法指令修复

文件：

```text
common/utils/time_meas.h
```

**原因**：K3 当前内核没有向用户态开放 RISC-V cycle counter。执行 `rdcycle` 会收到 `SIGILL`。RV64 分支改用：

```c
clock_gettime(CLOCK_MONOTONIC_RAW, &ts)
```

返回纳秒值，使现有时间换算继续有效。

### 12.5 RV64 asn1c 低优化构建

文件：

```text
cmake_targets/tools/build_helper
```

**原因**：OAI 固定版本依赖的 asn1c 0.9.29 在 K3 上高优化构建后，生成 M2AP/M3AP/NGAP/S1AP/X2AP 源码时段错误，继而出现 `ANY_aper.c` 不存在。补丁在 RV64 上以低优化和调试信息构建 asn1c。

### 12.6 SIMDe 获取回退

文件：

```text
cmake_targets/tools/build_helper
```

**原因**：GitHub TLS 中断会让依赖安装失败。修改后先复用 `/usr/include/simde`，克隆失败时重试，最后回退 `apt-get install libsimde-dev`。

### 12.7 LDPC 分段编码器标量回退

涉及：

```text
openair1/PHY/CODING/nrLDPC_coding/nrLDPC_coding_segment/
```

**原因**：原实现包含 `_mm256_*`、`__m256i` 和 `__m64` 等 x86 intrinsic。RV64 分支使用等价标量位操作，首先保证正确性和可编译性。

### 12.8 排除不需要的 LTE Turbo SSE 目标

文件：

```text
openair1/PHY/CODING/CMakeLists.txt
```

**原因**：`3gpplte_turbo_decoder_sse_8bit.c` 与 `16bit.c` 是旧 LTE SSE 路径，不是 NR gNB/nrUE RFsim 的必要前置。RV64 构建排除这些目标，避免无意义移植 x86 SSE。

### 12.9 CPU-only 插件链接符号

涉及：

```text
plugins/CMakeLists.txt
plugins/common/src/cpu_only_stubs.c
common/utils/load_module_shlib.c
```

**原因**：禁用 GPU 实现后，OAI 主程序仍需要插件接口符号。空实现保证链接完整，并明确拒绝 GPU 功能。

### 12.10 DFT 动态库 RV64 实现

文件：

```text
openair1/PHY/TOOLS/oai_dfts.c
```

**原因**：文件内部已通过 SIMDe 使用可移植接口，但最外层预处理条件只允许 x86，导致 RV64 生成空的 `libdfts.so`，运行时找不到 `dft_implementation`。补丁把 `__riscv` 加入编译条件。

### 12.11 其他 RV64 类型、对齐和加载调整

补丁还修改 PHY、RU、UE、PRACh、ULSCH、MAC 和 E2AP 中与架构、SIMD类型、对齐或插件加载有关的条件。逐项精确差异可直接查看：

```bash
less patches/openairinterface5g.patch
```

---

## 13. 安装 K3 CPU-only 基础依赖

方式 A 或 B 完成后执行：

```bash
cd ~/sionna-rk
./scripts/configure-system.k3.sh
```

脚本安装通用工具、`libcjson-dev` 和 `libzmq3-dev`，不会安装 NVIDIA 软件。

可选 Python 环境：

```bash
cd ~/sionna-rk
python3 -m venv .venv-cpu
source .venv-cpu/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements_cpu.txt
```

Python wheel 不齐全时可以暂时跳过，OAI 原生 gNB/nrUE 构建不依赖这一虚拟环境。

---

## 14. 下载固定版本 OAI 并自动应用 RV64 补丁

确认目标目录不存在：

```bash
cd ~/sionna-rk
test ! -e ext/openairinterface5g && echo "OAI destination is clean"
```

下载但暂不构建：

```bash
export SIONNA_RK_CPU_ONLY=1

./scripts/quickstart-oai.sh \
  --no-build \
  --source "$HOME/sionna-rk" \
  --dest "$HOME/sionna-rk/ext/openairinterface5g"
```

脚本会：

1. 克隆 OAI `2025.w34`；
2. 应用 `patches/openairinterface5g.patch`；
3. 初始化全部子模块；
4. 创建 `ext/openairinterface5g/plugins -> ~/sionna-rk/plugins` 符号链接。

验证：

```bash
git -C ext/openairinterface5g status --short --branch
git -C ext/openairinterface5g submodule status
ls -ld ext/openairinterface5g/plugins
readlink -f ext/openairinterface5g/plugins
```

检查关键修改：

```bash
grep -n 'riscv64' ext/openairinterface5g/CMakeLists.txt
grep -n 'CLOCK_MONOTONIC_RAW' ext/openairinterface5g/common/utils/time_meas.h
grep -n '__riscv' ext/openairinterface5g/openair1/PHY/TOOLS/oai_dfts.c
```

---

## 15. 安装 OAI 依赖

```bash
cd ~/sionna-rk
export SIONNA_RK_CPU_ONLY=1

./scripts/build-oai-native.sh \
  --install-deps \
  ext/openairinterface5g
```

该阶段会安装 OAI 依赖并构建 RV64 asn1c。不要看到几分钟无输出就中断。

如果 SIMDe 网络下载失败：

```bash
sudo apt-get update
sudo apt-get install -y libsimde-dev
```

然后重新运行依赖安装。

---

## 16. 构建 CPU-only gNB 和 nrUE

```bash
cd ~/sionna-rk
export SIONNA_RK_CPU_ONLY=1
./scripts/build-oai-native.sh ext/openairinterface5g
```

另一个本地终端可以观察编译进程：

```bash
ps -eo pid,etime,pcpu,pmem,stat,comm,args \
  | grep -E '[c]c1|[c]make|[g]make'
```

大型 DFT 文件可能持续约 10 分钟占用一个 CPU 核而没有输出。只要 `cc1` 仍消耗 CPU，就不要中断。

成功标志：

```text
Built target nr-softmodem
Built target nr-uesoftmodem
```

---

## 17. 构建产物检查

```bash
cd ~/sionna-rk/ext/openairinterface5g/cmake_targets/ran_build/build
ls -lh nr-softmodem nr-uesoftmodem libdfts.so
file nr-softmodem nr-uesoftmodem libdfts.so
```

必须是 RV64 ELF。

检查 DFT 符号：

```bash
nm -D libdfts.so | grep -E ' (dft|idft)_implementation$'
```

预期：

```text
T dft_implementation
T idft_implementation
```

检查无 NVIDIA 依赖：

```bash
ldd nr-softmodem | grep -Ei 'cuda|nvidia|tensorrt' || echo "gNB has no NVIDIA dependency"
ldd nr-uesoftmodem | grep -Ei 'cuda|nvidia|tensorrt' || echo "nrUE has no NVIDIA dependency"
```

---

## 18. gNB 单进程冒烟测试

```bash
cd ~/sionna-rk/ext/openairinterface5g/cmake_targets/ran_build/build

sudo timeout 20s ./nr-softmodem \
  -O ../../../ci-scripts/conf_files/gnb.sa.band78.106prb.rfsim.conf \
  --rfsim \
  --rfsimulator.serveraddr server \
  --phy-test \
  --noS1 \
  --gNBs.[0].min_rxtxtime 6 \
  > /tmp/k3-gnb-smoke.log 2>&1

status=$?
echo "gNB smoke exit=$status"
tail -80 /tmp/k3-gnb-smoke.log
```

预期退出码 `124`，表示运行满 20 秒后由 `timeout` 正常终止。

异常码参考：

- `132`：非法指令；
- `134`：断言/SIGABRT；
- `139`：段错误；
- `124`：本测试预期的超时终止。

---

## 19. 在同一块 K3 打开两个本地终端

在 K3 桌面打开两个 Terminal 窗口，分别称为“终端 1”和“终端 2”。不需要 SSH。

两个终端都从仓库根目录操作。不要直接执行 build 目录里的二进制，也不要在命令后加 `&`、`tee` 或 `timeout`。新版入口使用 systemd 独立托管 gNB 和 nrUE，使它们脱离 GNOME Console/ptyxis 的终端 scope；关闭日志窗口或打开新终端不会结束 softmodem。

先清理旧的托管服务并检查环境：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh stop
./scripts/run-k3-rfsim.sh check
```

如果检查结果仍列出 `nr-softmodem` 或 `nr-uesoftmodem`，说明它不是本手册启动的托管实例。回到其原终端按 `Ctrl+C` 停止后，再次执行 `check`。不要使用 `pkill -9` 或 `kill -9`。

如果原终端已经丢失，先记录进程树，再只结束真正的顶层实例：

```bash
ps -eo pid,ppid,stat,comm,args | grep -E 'nr-softmodem|nr-uesoftmodem|gdb|strace' | grep -v grep
```

OAI 会派生同名后台辅助进程，因此不要按进程名批量结束。正常情况下只使用本脚本的 `ue-stop`、`gnb-stop` 或 `stop`。

---

## 20. 终端 1 启动 gNB

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh gnb-start
```

预期立即返回：

```text
Running as unit: k3-rfsim-gnb.service
gNB is managed by systemd and survives terminal closure.
```

在同一个终端查看实时日志：

```bash
./scripts/run-k3-rfsim.sh gnb-log
```

此时按 `Ctrl+C` 只退出日志查看，不会停止 gNB。重新执行 `gnb-log` 可以再次查看。

保持该进程运行。预期：

```text
Running as server waiting opposite rfsimulators to connect
TYPE <CTRL-C> TO TERMINATE
No connected device, generating void samples...
```

---

## 21. 终端 2 启动 nrUE

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh ue-start
```

预期立即返回：

```text
Running as unit: k3-rfsim-nrue.service
nrUE is managed by systemd and survives terminal closure.
```

查看 UE 实时日志：

```bash
./scripts/run-k3-rfsim.sh ue-log
```

`ue-start` 只有在 gNB 服务为 active、4043 端口已监听并且没有旧 nrUE 时才会启动。按 `Ctrl+C` 只退出日志查看，不会停止 UE。

预期：

```text
Trying to connect to 127.0.0.1:4043
Connection to 127.0.0.1:4043 established
Initial sync: pbch decoded sucessfully
Initial sync successful, PCI: 0
UE synchronized!
```

gNB 终端应出现：

```text
Client connects from ::ffff:127.0.0.1
UE RNTI 1234 ... in-sync
```

---

## 22. 成功判据与停止顺序

成功必须同时满足：

1. `check` 显示两个 systemd 服务均为 `active`；
2. RFsim TCP 连接建立；
3. PBCH 解码成功；
4. `Initial sync successful`；
5. `UE synchronized`；
6. gNB 出现 UE `in-sync` 或 HARQ/LDPC 统计；
7. 没有 SIGILL、SIGSEGV 或断言失败；
8. ELF 为 RV64 且无 NVIDIA 动态依赖。

停止时必须先停止 nrUE 服务，再停止 gNB 服务：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh ue-stop
./scripts/run-k3-rfsim.sh gnb-stop
```

也可一次停止两者：

```bash
./scripts/run-k3-rfsim.sh stop
```

最后检查：

```bash
./scripts/run-k3-rfsim.sh check
```

---

## 23. 常见问题

### 23.1 `git apply --check` 失败

检查：

```bash
cd ~/sionna-rk
git rev-parse --short HEAD
git status --short
```

必须从干净的 `0abdce3` 开始。不要用 `--reject` 强行应用。

### 23.2 `ubuntu26.04 is not supported`

说明 OAI 补丁未正确应用：

```bash
grep -n 'ubuntu26.04' \
  ~/sionna-rk/ext/openairinterface5g/cmake_targets/tools/build_helper
```

### 23.3 ASN.1 生成阶段段错误和 `ANY_aper.c` 缺失

缺文件是 asn1c 崩溃的后果。检查：

```bash
file /opt/asn1c/bin/asn1c
/opt/asn1c/bin/asn1c -h | head
```

重新运行 `--install-deps`，不要手工创建空的 `ANY_aper.c`。

### 23.4 `Illegal instruction`

```bash
grep -n 'CLOCK_MONOTONIC_RAW' \
  ~/sionna-rk/ext/openairinterface5g/common/utils/time_meas.h
```

没有匹配说明 `rdcycle` 修复未应用。

### 23.5 nrUE 在 `Scanning GSCN` 后显示 `Killed`，退出码为 137

`137 = 128 + 9`，表示进程收到 `SIGKILL`。它不等于段错误，也不能据此判断 PBCH/SSB 扫描代码崩溃。先执行：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh check
free -h
sudo dmesg --ctime | tail -80
journalctl -k --since '-5 min' --no-pager | tail -100
```

若内核日志没有 `Out of memory` 或 `Killed process`，通常是旧的调试/限时命令、批量 `pkill -9`，或同时运行多份 softmodem 所致。还要注意：使用 `命令 2>&1 | tee 日志 &` 会建立后台管道，终端显示的 `Done` 是整个后台作业结束提示；必须用 `PIPESTATUS` 区分 softmodem 与 tee 的状态。推荐直接使用第 20、21 节的前台脚本。

本次 K3 回归使用新入口运行 nrUE 30 秒，日志依次出现 `Connection ... established`、`Initial sync successful` 和 `UE synchronized`；限时测试返回 `124`。另一次原生命令连续运行 60 秒也正常。这证明同步路径可以工作，但后续又通过信号跟踪确认了一个独立的退出清理错误，修复见下一节。

### 23.6 Ctrl+C 后出现 `Segmentation fault` 或管道异常结束

GDB 调用栈定位到：

```text
#0  0x0000000000000000
#1  free_plugins() at plugins/common/src/plugins.c:33
#2  main() at executables/nr-uesoftmodem.c:639
```

CPU-only 模式没有加载可选的 `libdemapper.so`，因此 `demapper_interface.shutdown` 为空；旧代码仍无条件调用 `free_demapper_lib()`，最终跳转到地址 0。RV64 补丁把释放逻辑修改为：

```c
if (demapper_interface.shutdown)
    free_demapper_lib(&demapper_interface);
if (receiver_interface.shutdown)
    free_receiver_lib(&receiver_interface);
```

重新构建 nrUE：

```bash
cd ~/sionna-rk/ext/openairinterface5g/cmake_targets/ran_build/build
cmake --build . --target nr-uesoftmodem -j2
```

修复后的验证结果：RFsim 连接成功、`Initial sync successful`、`UE synchronized`；向 nrUE 主进程发送 SIGINT 后打印 `Returned from ITTI signal handler` 和 `oai_exit=1`，最终退出码为 `0`，无残留 UE，内核没有新增 SIGSEGV。

### 23.7 `journalctl` 日志文件找不到

路径必须是 `/tmp`，不是 `/tem`。先启动监控，再复现问题，最后停止监控：

```bash
sudo journalctl -f > /tmp/k3-system-live.log 2>&1 &
JOURNAL_PID=$!
# 在另一个终端复现问题
kill "$JOURNAL_PID"
tail -100 /tmp/k3-system-live.log
```

### 23.8 本地新终端中 nrUE 约 13 秒后自动显示 `Killed`

本次实测发现直接在 GNOME Console 中以前台 `sudo` 启动时，两次 sudo 会话分别为 `07:25:35—07:25:48` 和 `07:26:26—07:26:39`，都在约 13 秒后结束；同期没有 OOM、systemd-oomd 杀进程记录或 cgroup 内存上限。SSH 环境中同一二进制可以运行，但这种差异说明不能再让实时 softmodem 依赖图形终端的 `ptyxis` scope。

最新版 `run-k3-rfsim.sh` 使用两个独立系统服务：

```text
k3-rfsim-gnb.service
k3-rfsim-nrue.service
```

确认方式：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh check
sudo systemctl show k3-rfsim-nrue.service \
  -p ActiveState -p MainPID -p ControlGroup -p Result
```

正常结果应包含：

```text
ActiveState=active
ControlGroup=/system.slice/k3-rfsim-nrue.service
Result=success
```

不要再直接运行 `sudo ./nr-uesoftmodem ...`。也不要把 `ue-log` 误认为 UE 本体：`ue-log` 只是日志查看器，按 Ctrl+C 或关闭该终端不会停止由 systemd 托管的 UE。真正停止 UE 必须执行：

```bash
./scripts/run-k3-rfsim.sh ue-stop
```

本次回归中，托管的 UE 超过 2 分钟仍为 `active`，成功出现 `Connection ... established`、`Initial sync successful` 和 `UE synchronized`；主进程父 PID 为 1，cgroup 为 `/system.slice/k3-rfsim-nrue.service`，已跨过此前固定的 13 秒退出窗口。

### 23.5 `dft_implementation not found`

```bash
nm -D ~/sionna-rk/ext/openairinterface5g/cmake_targets/ran_build/build/libdfts.so \
  | grep -E ' (dft|idft)_implementation$'
```

没有符号则重新构建：

```bash
cd ~/sionna-rk/ext/openairinterface5g/cmake_targets/ran_build/build
cmake --build . --target dfts -j2
```

### 23.6 nrUE 不能连接 4043

确认 gNB 先运行：

```bash
pgrep -a nr-softmodem
sudo ss -lntp | grep ':4043'
```

单板模式 nrUE 地址必须是 `127.0.0.1`。

### 23.7 使用 `tee` 后错误被隐藏

运行前执行：

```bash
set -o pipefail
```

管道结束后立即检查：

```bash
echo "program=${PIPESTATUS[0]} tee=${PIPESTATUS[1]}"
```

---

## 24. UE 发送 `hello`、gNB 返回 `world` 的双向数据实验

前面的 `Initial sync successful` 和 `UE synchronized` 证明物理层仿真已经同步；本节再发送实际的 UDP 应用数据，验证两条用户面隧道之间能够双向传递消息。

### 24.1 前置条件

先确认 gNB、nrUE 和两个隧道接口均存在：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh check
ip -br addr show oaitun_enb1
ip -br addr show oaitun_ue1
```

应看到两个服务均为 `active`，并且接口地址分别为：

```text
oaitun_enb1  10.0.1.1/24
oaitun_ue1   10.0.1.2/24
```

若服务尚未启动，按照第 20、21 节依次执行 `gnb-start` 和 `ue-start`，等 UE 日志出现 `UE synchronized` 后再测试。

### 24.2 一条命令完成双向实验

在 K3 新开一个终端执行：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh message-test
```

首次运行可能要求输入当前 K3 用户的 `sudo` 密码，这是因为同一 Linux 网络命名空间内同时存在 `10.0.1.1` 和 `10.0.1.2` 两个本地地址，测试需要临时放宽反向路径和本地源地址检查。密码输入时终端不回显字符，输入后按回车即可。

本次 K3 实测输出为：

```text
UE -> gNB: sent 5 bytes; received hello from 10.0.1.2:5002
gNB -> UE: sent 5 bytes; received world from 10.0.1.1:5001
PASS: bidirectional application messages crossed the OAI RFsim data bearer.
```

三行分别表示：

1. 绑定 `oaitun_ue1`、源地址 `10.0.1.2:5002` 的 UE 发送端发出 `hello`，绑定 `10.0.1.1:5001` 的 gNB 接收端收到它。
2. 绑定 `oaitun_enb1`、源地址 `10.0.1.1:5001` 的 gNB 发送端发出 `world`，绑定 `10.0.1.2:5002` 的 UE 接收端收到它。
3. 两个方向的内容、源地址和端口全部匹配，测试才打印 `PASS`。

脚本使用 `SO_BINDTODEVICE` 强制数据从指定 OAI 隧道接口发出，避免 Linux 因两个地址都在本机而走本地回环捷径。它在开始时保存下列内核参数，在退出、失败或按 Ctrl+C 时通过 `trap` 自动恢复，因此不需要把这些设置永久写入 `/etc/sysctl.conf`：

```text
net.ipv4.conf.*.rp_filter
net.ipv4.conf.*.accept_local
```

这项实验验证的是 OAI RFsim 用户面承载上的应用数据双向传递。它比仅看到 TCP 4043 已连接或 UE 已同步更进一步，但仍是单板软件无线仿真，不等于已经接入真实射频前端、真实手机或完整 5GC。

### 24.3 修改消息内容或端口

测试实现位于：

```text
~/sionna-rk/scripts/k3-rfsim-message-test.sh
```

文件末尾的两次 `exchange(...)` 调用分别定义 UE→gNB 与 gNB→UE。可以把 `b"hello"`、`b"world"` 改成其他 ASCII/UTF-8 字节串，也可以修改 `5001`、`5002`，但同一方向的接收端口和目标端口必须一致，且端口不能被其他程序占用。修改完成后执行：

```bash
bash -n ~/sionna-rk/scripts/k3-rfsim-message-test.sh
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh message-test
```

---

## 25. 可以调节的仿真参数

可以调节，但参数分属不同层次。改之前先执行 `./scripts/run-k3-rfsim.sh stop`，修改后重新启动 gNB 和 nrUE。频段、子载波间隔、带宽和频率等成组参数必须有效并保持两端一致，否则最常见的结果就是 UE 停在 `Scanning GSCN` 或无法同步。

### 25.1 RFsim 连接和传输参数

这些参数来自当前 OAI 源码的 `radio/rfsimulator/simulator.c` 和 `radio/rfsimulator/README.md`：

| 参数 | 当前/默认值 | 作用 | 修改要求 |
|---|---:|---|---|
| `--rfsimulator.serveraddr` | UE 为 `127.0.0.1`，gNB 为 `server` | 指定客户端连接地址，或让进程作为 RFsim 服务端 | 单板保持当前值；双板时 UE 改为 gNB 板的局域网 IP |
| `--rfsimulator.serverport` | `4043` | RFsim TCP 监听/连接端口 | 两端必须相同，且端口未被占用 |
| `--rfsimulator.wait_timeout` | `1` 秒 | 没有 UE 数据时的等待超时 | 可增大以减少频繁超时唤醒 |
| `--rfsimulator.prop_delay` | `0` ms | 模拟接收路径传播时延 | 先小幅增加；过大会超出同步/时序窗口 |
| `--rfsimulator.offset` | `0` 个采样 | 用采样点数设置时延偏移 | 与 `prop_delay` 都表示时延，实验时选择一种 |
| `--rfsimulator.options saviq` | 关闭 | 把发出的 IQ 样本保存到文件 | 文件会很快变大，确认存储空间后使用 |
| `--rfsimulator.IQfile` | `/tmp/rfsimulator.iqs` | `saviq` 的输出路径 | 只在启用 `saviq` 时生效 |

单板改端口的示例是同时在 gNB 与 UE 的启动命令中添加：

```text
--rfsimulator.serverport 4050
```

只改一端会导致连接失败。

### 25.2 信道模型参数

默认 RFsim 不启用信道模型，等效于先验证理想的数据通路。启用入口为：

```text
--rfsimulator.options chanmod
```

当前源码还暴露 `modelname`、`ploss`、`forgetfact` 等高级参数；可用模型枚举包括 `AWGN`、`SCM_A` 和 `TDL_A`～`TDL_E` 等。当前版本启用 `chanmod` 后会从 OAI 信道配置表加载 `rfsimu_channel_ue0`、`rfsimu_channel_enB0` 等条目，因此不应只在命令行随意填写一个模型名。应先阅读并配套修改：

```text
~/sionna-rk/ext/openairinterface5g/openair1/SIMULATION/TOOLS/DOC/channel_simulation.md
~/sionna-rk/ext/openairinterface5g/openair1/SIMULATION/TOOLS/sim.h
~/sionna-rk/ext/openairinterface5g/radio/rfsimulator/simulator.c
```

| 参数/配置 | 作用 | 风险 |
|---|---|---|
| `modelname` / 信道表模型类型 | AWGN、SCM、TDL 等传播模型 | 模型名称或信道条目不匹配会直接报 `Channel model ... not found` |
| `ploss` | 路径损耗，单位 dB | 损耗过大时 UE 无法检测 SSB 或误码显著增加 |
| `forgetfact` | 信道遗忘因子，范围 0～1 | 改变相邻时刻信道相关性，需结合模型理解结果 |
| `prop_delay` / `offset` | 传播时延 | 过大会破坏随机接入及上下行时序 |
| `saviq` / `IQfile` | 保存 IQ 数据用于离线分析或回放 | 高采样率下文件占用很大，长时间运行可能写满磁盘 |

建议按“无 `chanmod` 基线 → AWGN → 单个 TDL 模型 → 逐步增加损耗或时延”的顺序做对照实验，每次只改变一个变量并保存 gNB/UE 日志。

### 25.3 NR 载波和帧结构参数

gNB 的主要配置文件是：

```text
~/sionna-rk/ext/openairinterface5g/ci-scripts/conf_files/gnb.sa.band78.106prb.rfsim.conf
```

| 配置项 | 当前值 | 含义 |
|---|---:|---|
| `physCellId` | `0` | NR 物理小区 ID |
| `absoluteFrequencySSB` | `621312` | SSB 的 NR-ARFCN |
| `dl_absoluteFrequencyPointA` | `620040` | 下行 Point A 的 NR-ARFCN |
| `dl_subcarrierSpacing` | `1` | 下行 numerology 1，即 30 kHz |
| `dl_carrierBandwidth` | `106` | 下行 106 PRB |
| `ul_frequencyBand` | `78` | 上行频段 n78 |
| `ul_subcarrierSpacing` | `1` | 上行 numerology 1，即 30 kHz |
| `ul_carrierBandwidth` | `106` | 上行 106 PRB |
| `pMax` | `20` | UE 最大发射功率配置，单位 dBm |
| `prach_ConfigurationIndex` | `98` | PRACH 时频配置索引 |
| `preambleReceivedTargetPower` | `-104` | PRACH 前导目标接收功率 |
| `preambleTransMax` | `6` | PRACH 最大重传等级配置 |

nrUE 启动命令中的对应参数为：

| UE 参数 | 当前值 | 含义 |
|---|---:|---|
| `-r` | `106` | 资源块数，需匹配 gNB 带宽 |
| `--numerology` | `1` | 子载波间隔编号，需匹配 gNB |
| `--band` | `78` | NR 频段，需匹配 gNB |
| `-C` | `3619200000` | UE 启动搜索频率提示；当前 `--phy-test` 流程还会依据 gNB 配置重配，不能把它单独当作最终运行频率 |
| `--ue-rxgain` | `140` | UE 接收增益参数 |
| `--ue-txgain` | `0` | UE 发射增益参数 |

频段、NR-ARFCN、Point A、SSB、PRB 数和 numerology 之间受 3GPP 组合约束，不能只把某个数字改成期望频率。若要从 n78/106 PRB/30 kHz 改为另一套配置，最稳妥的做法是选择 OAI 已提供的同频段模板，同时修改 gNB 配置和 `run-k3-rfsim.sh` 中的 UE 参数，然后重新验证同步与 `message-test`。

### 25.4 接入、标识和时序参数

还可以修改以下内容：

| 参数 | 作用 | 说明 |
|---|---|---|
| `--gNBs.[0].min_rxtxtime 6` | gNB 收发处理提前量 | K3 性能有限，减小可能导致实时处理赶不上；没有测量依据不要改 |
| `tracking_area_code` | 跟踪区码 TAC | 接入 5GC 时需与核心网规划一致 |
| `mcc`、`mnc`、`mnc_length` | PLMN 标识 | 接入 5GC/真实 UE 时需一致 |
| AMF 地址 | gNB 的 N2 对端 | 当前带 `--noS1 --phy-test`，不会因此建立完整 5GC 会话 |
| PRACH 参数 | 随机接入时机、功率和重传 | 需要与 TDD/带宽/小区配置成套验证 |
| 天线端口/层数 | MIMO 与收发链数量 | 增加后会明显增加 K3 的 CPU 和内存负载 |
| `--T_stdout` | 控制终端日志 | 只影响日志输出，不改变无线算法结果 |

### 25.5 推荐的参数实验方法

每次实验按下面流程进行，便于定位是哪一个参数导致变化：

```bash
cd ~/sionna-rk
./scripts/run-k3-rfsim.sh stop

# 备份将要修改的文件
cp ext/openairinterface5g/ci-scripts/conf_files/gnb.sa.band78.106prb.rfsim.conf \
   ~/gnb.sa.band78.106prb.rfsim.conf.before-experiment
cp scripts/run-k3-rfsim.sh ~/run-k3-rfsim.sh.before-experiment

# 修改一个参数后检查脚本语法
bash -n scripts/run-k3-rfsim.sh

./scripts/run-k3-rfsim.sh gnb-start
./scripts/run-k3-rfsim.sh ue-start
./scripts/run-k3-rfsim.sh check
./scripts/run-k3-rfsim.sh message-test
```

同时保存本次参数和日志：

```bash
mkdir -p ~/k3-rfsim-results
cp ext/openairinterface5g/ci-scripts/conf_files/gnb.sa.band78.106prb.rfsim.conf \
   ~/k3-rfsim-results/gnb-experiment.conf
sudo journalctl -u k3-rfsim-gnb.service --no-pager \
  > ~/k3-rfsim-results/gnb-experiment.log
sudo journalctl -u k3-rfsim-nrue.service --no-pager \
  > ~/k3-rfsim-results/nrue-experiment.log
```

至少记录：修改前值、修改后值、是否同步、`message-test` 是否通过、运行持续时间、CPU/内存占用和错误日志。这样才能区分参数效果与偶发的调度或资源问题。

---

## 26. 成功后保存本机移植结果

```bash
cd ~/sionna-rk
git add -A
git commit -m "Port Sionna-RK CPU-only OAI flow to K3 RV64"
```

保存环境信息：

```bash
date
uname -a
cat /etc/os-release
git rev-parse HEAD
git -C ext/openairinterface5g rev-parse HEAD
sha256sum ~/sionna-rk-k3-rv64.patch
```

日志位于：

```text
/home/ubuntu/k3-rfsim-gnb.log
/home/ubuntu/k3-rfsim-nrue.log
```

---

## 27. 最终状态

```text
K3 RV64 / Ubuntu
  └─ 官方 Sionna-RK 0abdce3 + 本手册修改
       ├─ CUDA/TensorRT/NVIDIA Runtime/MPS：禁用
       ├─ OAI gNB：RV64 CPU 原生程序
       ├─ OAI nrUE：RV64 CPU 原生程序
       └─ 单板 RFsim 127.0.0.1:4043
            ├─ TCP 已连接
            ├─ PBCH 已解码
            ├─ PCI 已识别
            ├─ UE 已同步
            └─ hello/world 双向用户面消息测试通过
```

完成以上判据后，才可认为“禁用 NVIDIA 插件的 K3 RV64 CPU-only 移植”和“单块 K3 gNB/nrUE RFsim 仿真”均已复现成功。
